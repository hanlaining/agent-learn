# God-Agent 测试用例与验收标准

> 文档编号：D03
> 状态：Accepted
> 版本：v1.0.0
> 事实基线：`main@928fe38afa6d038e7fafbd6ff1c54c3c35d58793`
> 核心原则：测试通过不等于生产闭环；报告必须说明实际走过的入口、进程、文件和 Provider

## 1. 测试目标

本文件回答“用什么证据才能证明需求完成”。测试不以用例数量为目标，而是验证 D01 的十条核心不变量在正常、并发、崩溃、恢复、取消、越权和资源耗尽条件下仍成立。

以下三种结论必须严格区分：

- **组件正确**：类或模块在隔离条件下通过，证据最高为 E2。
- **生产组装正确**：通过 App Server 公共入口、真实持久化和真实 Coordinator 运行，证据可到 E3。
- **真实外部效果正确**：涉及真实 Provider、系统命令或外部服务时，必须另有受控实验；本地 Fake 不能替代。

## 2. 测试层级与执行环境

| 层级 | 目的 | 必须使用 | 禁止替代 | 证据上限 |
|---|---|---|---|---|
| L1 单元 | 状态转换、校验、序列化 | 确定性 fixture/clock | 用端到端掩盖边界错误 | E2 |
| L2 组件 | Engine、WAL、Lease、Scheduler 契约 | 真实组件组合、隔离目录 | 只断言 mock 被调用 | E2 |
| L3 生产集成 | 验证 `main` 的真实依赖接线 | 公共 RPC、生产 Store/WAL/Coordinator | 测试直接 new 内部类冒充生产组装 | E3 |
| L4 双实例 | 验证所有权和 fencing | 两个 App Server、共享隔离状态目录 | 单实例内模拟两个 owner | E3 |
| L5 Process Chaos | 验证真实崩溃恢复 | 子进程、真实 kill/restart、真实 JSON reload | `SimulatedProcessExit` | E3 |
| L6 Electron E2E | 验证用户可见主链 | 打包/开发壳、真实 IPC/窗口 | 纯 DOM 单测 | E3 |
| L7 容量与稳定性 | 找到瓶颈和安全范围 | 冻结机器、commit、配置、时长 | 把 Task 数写成 Agent 并发 | E3 |
| L8 Provider 实验 | 验证外部行为和费用 | 显式开关、白名单、小预算 | 默认 CI 读取 Key | E3/E4 |

每次 L3 以上运行必须记录：OS、CPU、内存、Node/Electron 版本、commit、工作树是否干净、配置、seed、开始/结束时间、原始报告路径与 SHA-256。

## 3. 通用判定规则

1. P0 用例不得跳过、重试到绿或标记 flaky。
2. 同一用例重复运行时，任何一次核心不变量失败都判失败。
3. 超时必须区分“系统按设计拒绝”“资源预算耗尽”“测试 Harness 失联”。
4. 未知副作用只能进入 `outcome_unknown`，不能按普通失败自动重放。
5. baseline 与消融组必须走同一生产入口，只能通过受控配置关闭被研究机制。
6. 日志中的类名、布尔标记和测试自报不能证明实际路径；须保留 RPC 轨迹、进程 PID、状态文件变化或调用审计。
7. 真实 Provider 测试失败不得用扩大预算反复碰运气；先形成最小复现并停止计费。

## 4. P0：Dynamic Engine Lease

| ID | 对应需求 | 场景与操作 | 必须断言 | 层级 |
|---|---|---|---|---|
| TC-DYNLEASE-001 | FR-DYN-005 | `start` 获取 Job Lease 后创建/推进父任务 | 每次持久提交携带同一活动 fencing token | L2/L3 |
| TC-DYNLEASE-002 | FR-DYN-005 | `resume`、`recover` 分别进入 ownership | 缺 Lease 不提交；被占用只等待、不污染状态 | L2/L3 |
| TC-DYNLEASE-003 | FR-DYN-005 | `provideFeedback` 与恢复并发 | 只有有效 Owner 接受反馈并生成一个新 attempt | L4 |
| TC-DYNLEASE-004 | FR-DYN-005 | `cancel` 与模型/工具返回竞争 | 形成唯一终态；旧回调不能复活 Job | L4/L5 |
| TC-DYNLEASE-005 | FR-LEASE-003 | 同 Job 的 Engine→AgentLoop 嵌套调用 | 复用 Lease context，不二次 acquire、不死锁 | L2 |
| TC-DYNLEASE-006 | FR-LEASE-003 | 持有 Job A 时嵌套执行 Job B | 立即拒绝并给出可诊断错误，A 不受污染 | L2 |
| TC-DYNLEASE-007 | FR-DYN-003 | Return claim→父 continuation→consume | 全链 fenced；只产生一个 Receipt 和一次最终交付 | L3/L5 |
| TC-DYNLEASE-008 | FR-LEASE-002 | 旧 Owner 在 Return boundary 迟到提交 | 100% 被拒，未生成重复 continuation | L4 |
| TC-DYNLEASE-009 | FR-LEASE-002 | 旧 Owner 在 Stage boundary 迟到提交 | 100% 被拒，阶段不倒退 | L4 |
| TC-DYNLEASE-010 | FR-LEASE-002 | 旧 Owner 在 Model commit 迟到提交 | 100% 被拒；远端结果按 WAL 事实处置 | L4/L5 |
| TC-DYNLEASE-011 | FR-LEASE-002 | 旧 Owner 在 Tool commit 迟到提交 | 100% 被拒；副作用未知时进入人工处置 | L4/L5 |
| TC-DYNLEASE-012 | FR-LEASE-002 | 旧 Owner 在 Cancel commit 迟到提交 | 100% 被拒，不能覆盖新 Owner 的终态 | L4 |
| TC-DYNLEASE-013 | FR-LEASE-001 | 两实例同时 `start`，1000 轮 | 每轮仅一个合法 Owner，无双推进 | L4 |
| TC-DYNLEASE-014 | FR-LEASE-003 | 两实例同时恢复同一非终态 Job | 一个恢复，另一个可观察等待；App Server 不退出 | L4/L5 |

P0 退出门槛：上述用例全绿；生产组装中 ownership 为必需依赖；`withActiveFencedCommit` 不允许在应受保护路径无 Lease 静默提交。

## 5. Process Chaos 故障矩阵

Harness 必须启动真实 App Server 子进程，共享一个新建的隔离临时状态目录；收到故障点落盘确认后强制结束 Owner 进程，等待 Lease 过期，再由第二进程通过公共 RPC 恢复。

| ID | kill 窗口 | 恢复后的唯一正确结果 |
|---|---|---|
| TC-CHAOS-001 | Model 请求提交前 | 允许安全发起一次请求 |
| TC-CHAOS-002 | Model 响应收到、结果持久化前 | 有远端幂等证据才查询/恢复，否则 `outcome_unknown`，禁止盲重放 |
| TC-CHAOS-003 | Tool 副作用执行前 | 允许安全执行一次 |
| TC-CHAOS-004 | Tool 副作用完成、结果持久化前 | 可查询工具才核验，否则 `outcome_unknown` |
| TC-CHAOS-005 | Return claim 后、父 continuation 前 | 新 Owner 继续一次，Receipt 唯一 |
| TC-CHAOS-006 | 父 continuation 后、Return consume 前 | 不重复父模型/最终交付，只补 consume |
| TC-CHAOS-007 | Workflow stage 计算完成、提交前 | 按 Invocation/Stage 事实恢复，不重复不可重放副作用 |
| TC-CHAOS-008 | fenced commit 完成、Lease release 前 | 新 Owner 看到已提交事实，不重复推进 |

每个窗口至少运行 5 个固定 seed 形成 GATE-40；完成调试后扩展到 GATE-100。每条报告必须包含旧/新 PID、kill 时间、Lease epoch/token、WAL 状态序列、调用次数和最终快照哈希。

## 6. WAL、幂等与人工处置

| ID | 对应需求 | 场景 | 验收标准 |
|---|---|---|---|
| TC-WAL-001 | FR-WAL-001 | 同 Invocation ID 重复进入模型提交 | 最多一次不可重试 POST |
| TC-WAL-002 | FR-WAL-001 | `response_received` 后重启 | 零重放提交已收响应 |
| TC-WAL-003 | FR-WAL-002 | `result_received` 后重启 | 零重放提交已收工具结果 |
| TC-WAL-004 | FR-WAL-003 | 请求/副作用结果无法确认 | 状态为 `outcome_unknown`，恢复循环停止 |
| TC-WAL-005 | FR-WAL-004 | 两个操作者并发 resolve | 只有一个版本匹配的处置成功 |
| TC-WAL-006 | FR-WAL-004 | 重复提交同一人工处置 | 幂等返回且审计只记录一次事实变化 |
| TC-WAL-007 | NFR-SEC-001 | 扫描快照、日志、报告 | 不含 Key、Authorization、Cookie、Token |

“零重复调用”只可限定于确定性本地 Provider/Tool；真实 Provider 必须另报请求 ID 和服务端查询能力。

## 7. 专家团、Tool、Skill 与 MCP

| ID | 对应需求 | 场景 | 验收标准 |
|---|---|---|---|
| TC-ROLE-001 | FR-TEAM-003 | Job 启动后升级 Role Pack | 运行中 Job 保持冻结版本，新 Job 使用新版本 |
| TC-ROLE-002 | FR-TEAM-004 | 串行、fan-out/fan-in、Review/仲裁 | 三种拓扑正常/阻塞/返工/恢复均可完成 |
| TC-ROLE-003 | FR-TEAM-005 | 阶段声明 Skill，Role/Job 分别收紧权限 | 最终权限取交集，越权读取被拒 |
| TC-ROLE-004 | FR-TEAM-006 | 多专家意见冲突 | 保留证据、置信度、反例和裁决依据 |
| TC-CAP-001 | FR-CAP-005 | Job A 耗尽 Tool 次数预算 | A 的新调用停止，Job B 继续运行 |
| TC-CAP-002 | FR-CAP-005 | MCP 输出/墙钟/并发超限 | 只取消归属调用，资源最终释放 |
| TC-CAP-003 | FR-CAP-006 | 共享 MCP Server 崩溃 | 相关调用得到确定状态，其他 Job 不被串改 |
| TC-CAP-004 | FR-CAP-001 | Tool 参数非法或 Scope 越界 | 副作用发生前拒绝并留下审计 |
| TC-CAP-005 | FR-CAP-003 | Skill 目录链接/路径逃逸 | 拒绝读取 workspace 外内容 |

## 8. 持久化与恢复

| ID | 对应需求 | 场景 | 验收标准 |
|---|---|---|---|
| TC-DATA-001 | FR-DATA-001 | 逐版加载 v1-v6 fixture | 迁移确定、语义不丢、再次保存为当前版本 |
| TC-DATA-002 | FR-DATA-003 | 写入中断、临时文件残留 | 选择最近完整快照，不接受半写文件 |
| TC-DATA-003 | FR-DATA-003 | 截断、改字节、校验和错误 | 进入只读恢复模式，保留损坏源文件 |
| TC-DATA-004 | FR-DATA-003 | 从最近 N 份备份恢复 | 恢复结果可校验，明确丢失窗口 |
| TC-DATA-005 | FR-DYN-006 | 重启前存在等待/依赖/可运行任务 | 从持久事实重建 queue，不丢等待原因和 deadline |
| TC-DATA-006 | NFR-REL-003 | 启动 fact-only recovery | 不调用模型、不执行 Tool、不产生外部费用 |
| TC-DATA-007 | FR-DATA-002 | 10 万历史对象查询活动 Job | 使用索引/分区，不进行全量对象扫描 |

## 9. Electron 真机与视觉验收

| ID | 场景 | 验收标准 |
|---|---|---|
| TC-UI-001 | 启动、握手、新建 Chat、发送 | 页面可交互，流式内容顺序稳定，最终回答一次 |
| TC-UI-002 | 两 Chat 并行并切换 | 不串内容/状态，切换不取消后台任务 |
| TC-UI-003 | 取消、重启、历史恢复 | 状态与 App Server 一致，终态不倒退 |
| TC-UI-004 | 父子树展开后再次点击 | 同一控制可收起，焦点和键盘操作可用 |
| TC-UI-005 | 新建 Chat 的日期分组 | 立即出现在“今天”，刷新前后不跳组 |
| TC-UI-006 | waiting/feedback/error | 等待反馈为橙色，真实失败为红色，文案说明下一步 |
| TC-UI-007 | 长中文、长代码、窄窗、DPI/主题 | 无遮挡、漂移、不可达按钮和错误截断 |
| TC-UI-008 | 父 Agent 收到子 Return | UI 显示父级继续/等待原因，不把正常反馈状态报错 |

关键页面建立截图基线，但像素差异只能发现变化，不能替代语义和交互断言。视觉变更需人工确认基线后更新。

## 10. 容量、稳定性与压爆测试

### 10.1 工作负载

| ID | 类型 | 建议初始配置 | 目的 |
|---|---|---|---|
| TC-LOAD-001 | Spike | 1→目标并发瞬时跃升，5 分钟 | 找排队、拒绝和崩溃边界 |
| TC-LOAD-002 | Steady | 目标并发持续 30 分钟 | 测稳定吞吐和延迟 |
| TC-LOAD-003 | Short-soak | 70% 目标并发持续 2 小时 | 找内存、句柄、队列累积 |
| TC-LOAD-004 | Recovery drain | 运行中 kill Owner，重启后清空积压 | 测恢复吞吐和重复副作用 |
| TC-LOAD-005 | Storage pressure | 1k/10k/100k 历史对象 | 测序列化、索引和恢复时间 |
| TC-LOAD-006 | Noisy neighbor | Job A 输出/Tool/MCP 压满，Job B 正常 | 验证预算和隔离 |

逐级阶梯为 1、2、4、8、16……并发；一旦触发停止条件，不继续扩大。每一级预热 2 分钟、测量至少 10 分钟并重复 3 次。若使用 Fake Provider，只能报告 Runtime 容量，不得外推模型服务容量。

### 10.2 指标

- 吞吐：完成 Task/Run/Job 每分钟，分母定义必须固定。
- 延迟：排队、模型、工具、提交、端到端的 P50/P95/P99。
- 正确性：重复 Model/Tool、双 Owner、丢 Return、非法状态转换。
- 恢复：RTO、积压 drain 时间、恢复成功率、未知结果数。
- 资源：RSS、Heap、CPU、Event Loop lag、句柄、状态文件体积。
- 退化：相对上一稳定台阶的吞吐下降和 P95 增幅。

停止条件：双 Owner或重复副作用 > 0；未捕获异常；RSS 连续三个窗口单调增长且 GC 后不回落；错误率 > 1%；P95 超基线 2 倍；恢复积压无法收敛。停止后保存现场，不以“压爆本就会失败”为由忽略正确性错误。

安全承载量定义为：同一机器、commit、配置下，连续三轮均未触发停止条件的最高一级；报告有效期随代码或机器变化而失效。

## 11. 真实 Provider 受控实验

默认测试、CI 和 Process Chaos **禁止**读取或调用真实 Provider。启用时必须同时满足：

1. `REAL_PROVIDER_EXPERIMENT=1` 之类的显式开关。
2. 模型白名单、最大请求数、Token、金额、并发和总时长。
3. 使用专用低额度凭据，凭据只来自进程环境且不落盘。
4. 运行前打印预计上限并由实验者确认；达到任一上限立即停止。
5. 保存脱敏 request ID、HTTP 类别、重试/查询轨迹和实际费用。
6. 不向 Provider 上传未授权源码、个人信息或历史对话。

首轮只验证 10–30 个调用的小样本协议行为，不用于得出性能普遍结论。

## 12. Go / No-Go 门槛

| 里程碑 | Go 条件 | 任一 No-Go 条件 |
|---|---|---|
| P0 Lease 完成 | TC-DYNLEASE 全绿；生产依赖必需；双实例 1000 轮无双 Owner | 任一状态写可无 Lease 静默发生 |
| Process GATE | GATE-40/100 全绿，八窗口都是真 kill/restart | 模拟退出、直接调 Store、路径自报 |
| 专家团底座 | 三模板不改 Coordinator 即可运行并恢复 | 角色/权限/拓扑仍硬编码 |
| Beta 演示 | Electron 主链、恢复、反馈、错误语义全绿 | 演示依赖手改状态或真实 Key |
| 论文实验 | 冻结版本、重复/消融/统计/原始数据/外部复核齐全 | 只有成功截图或单次汇总 |

## 13. 原始报告与失败复现

每条 JSONL 至少包含：

```json
{
  "schemaVersion": 1,
  "testId": "TC-CHAOS-005",
  "runId": "immutable-id",
  "commit": "full-sha",
  "dirty": false,
  "seed": 17,
  "environment": {},
  "configDigest": "sha256:...",
  "startedAt": "ISO-8601",
  "finishedAt": "ISO-8601",
  "result": "passed|failed|inconclusive",
  "ownerHistory": [],
  "walTransitions": [],
  "modelCalls": 0,
  "toolEffects": 0,
  "artifacts": [],
  "limitations": []
}
```

失败报告必须给出单条复现命令、fixture/seed、期望、实际、首个错误事件、相关状态文件副本和哈希。汇总 CSV 必须由 JSONL 生成，禁止手工修改论文数字。

## 14. 手工演示验收脚本

1. 在全新隔离状态目录启动应用，展示 commit 与 Provider 类型。
2. 新建两个 Chat 并行执行，取消其中一个，证明互不影响。
3. 创建一个父任务并产生至少两个有依赖的子任务，展示 Evidence 和 Return。
4. 让一个子任务进入等待反馈；确认橙色状态，提交反馈后父 Agent 继续。
5. 在指定安全故障点终止 App Server，重启并展示恢复轨迹、Lease Owner 和无重复调用计数。
6. 打开 `outcome_unknown` 示例，演示默认不重放与带版本的人工处置。
7. 导出脱敏证据包，现场从原始 JSONL 重建汇总。
8. 主动说明限制：当前 Provider 类型、未完成项、数据规模和结论边界。

演示成功不能替代自动化门禁；演示失败必须记录进 D04，而不是重启后只展示成功的一次。

## 15. 当前基线裁决

- 当前集成候选在本机通过 `npm run check`、19/19 pretest、482/482 主测试；Snapshot 专项 24/24、Lease 专项 21/21。该组结论属于 E2，不因测试数量自动升级为 E3。
- Dynamic Engine 的 production ownership、同 Job 并发 Lease session、取消线性化和迟到 Model/Tool 结果栅栏已完成 E2；真实双 App Server 的 Dynamic 全边界矩阵尚未执行，因此不得写成 E3 完成。
- Snapshot v7 generation/state-capability CAS 已通过独立攻击矩阵：stale writer、discard reload、Store/数组混用、跨 persistence、公开 generation 篡改和 alternating getter 均 fail-closed。它是单文件本地 CAS，不是数据库事务或 exactly-once。
- Process Chaos Harness 已真实启动 App Server、强杀/重启三代 PID、通过公共 RPC 与原始 JSON 双重验证恢复，并证明 `return_god` 调用保持一次；只可计为 Team Workflow 的 TC-CHAOS-006 类 1 个 seed，即 GATE-40 **1/40**。
- 当前全局测试时钟会同时快进 Lease TTL 与 stale-lock 时间语义，报告也缺完整环境、fencing/WAL 序列和最终快照哈希；因此剩余 39/40 与报告治理仍是阻塞项。
- 本文中的 TC 表是验收合同；除本节明确列出的执行结果外，均不代表已经执行。

## 16. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-19 | v1.0.0 | 建立从组件到真实进程、容量和 Provider 的分层验收体系 |
| 2026-08-19 | v1.1.0 | 更新 Dynamic Lease、Snapshot CAS 与 Process Chaos 1/40 的分级证据及剩余反例 |

## 17. Research Artifact v0.1 验收矩阵（2026-08-20 追加）

本节只追加 Research Artifact v0.1 的集成验收合同，不覆盖前述历史裁决。矩阵中的“已通过”表示当前参考工作区中的对应检查已观察到通过；它不等于冻结 Artifact 已完成、不等于第三方已经复现，也不把测试全绿升级为端到端 exactly-once、完整 E3、GATE-40 完成或生产可用。

### 17.1 Manifest 完整性与负向门禁

| ID | 验收项 | 必须断言 | 当前证据/状态 |
|---|---|---|---|
| RA-MAN-001 | Manifest `create` | 递归收集除 Manifest 自身外的文件；记录规范相对路径、字节数、SHA-256、内容类型、冻结 commit、命令、UTC 起止时间、Node/OS 和 Provider 声明；文件按路径确定排序 | Manifest 专项 9/9，三层 Frozen Manifest 已创建并 verify |
| RA-MAN-002 | Manifest `verify` | 规范 JSON 可重读；当前文件集合、字节数、内容类型和 SHA-256 全部匹配；非规范序列化或自包含哈希被拒绝 | 入口已实现并通过专项；最终冻结包仍须逐包执行 `verify` |
| RA-MAN-003 | 内容篡改负例 | 任一已登记文件内容或字节数改变时，`verify` 非零退出 | 9/9 专项覆盖 |
| RA-MAN-004 | 文件缺失负例 | Manifest 已登记文件缺失时，`verify` 非零退出并报告相对路径 | 9/9 专项覆盖 |
| RA-MAN-005 | 未声明多余文件负例 | 创建 Manifest 后新增文件时，`verify` 非零退出 | 9/9 专项覆盖 |
| RA-MAN-006 | 路径负例 | 拒绝目录穿越、绝对路径、反斜杠、点路径段、重复路径、Manifest 自包含、符号链接和逃逸 Artifact 根目录 | 运行时校验与专项已覆盖；冻结包仍须在复制件上复做负向检查 |
| RA-MAN-007 | 凭据负例 | 拒绝敏感文件名/扩展名、高置信凭据内容，以及命令或正文中的绝对本机路径、环境变量值、Token、Key、密码或 Authorization；Provider 元数据必须声明 `realApiCalls=false`、`credentialsRead=false` | 9/9 专项覆盖正文绝对路径/GitHub Token 攻击；本轮未调用真实 Provider |

### 17.2 Process Chaos 门禁

| ID | 验收项 | 必须断言 | 当前证据/状态 |
|---|---|---|---|
| RA-PROC-001 | 运行时 Schema | `validateProcessChaosReport` 校验 Schema 版本、窄范围实验口径、三代 PID、两个观测窗口、Fake Provider 计数、最终 Job/Return 状态和禁止过度声明字段 | 独立验收首次 1/2、立即复跑 2/2；根因修复后员工连续四轮 2/2；总控独立复验 2/2 通过 |
| RA-PROC-002 | JSON Schema | 同一报告同时通过 `process-chaos-report.schema.json`；运行时校验与 JSON Schema 任一失败均不得判通过 | 修复后员工四轮与总控单轮成功报告均通过；Frozen/Clean 语义投影相同，首次超时仍保留 |
| RA-PROC-003 | PID 与临时资源清理 | Owner、reload、recovery 三代 PID 必须不同且在测试结束后均不可存活；`.transient/`、App Server 客户端、Fake Server 和活动 timer 必须清理 | 曾出现 Windows `EPERM` rename 和约 22.4 秒 fault point 等待超时；后者已完成机制修复，员工四轮及总控单轮均 2/2 且无残留，仍不构成跨环境稳定性证明 |
| RA-PROC-004 | 安全相对路径 | Process 报告中的 `statePath` 只能是 `runtime-state.json`，`rawReportPath` 只能是 `process-chaos-report.json`；绝对路径、反斜杠和点路径段必须被运行时 Schema 与 JSON Schema 同时拒绝 | 已通过正负向检查 |
| RA-PROC-005 | Process 原始状态公开边界 | 报告路径字段安全不代表原始状态内容已脱敏。v0.1 已选择从公开包排除含本机绝对计划路径的 raw `runtime-state.json`，只纳入 `process-chaos-report.json`、`runtime-leases.json`、环境、命令与限制；raw state 可由 Runner 在复核机重建，Claim 必须同步降级 | 已完成；Process Manifest 仅登记 `SCOPE.md`、报告和 Lease 三个公开文件并通过 verify，raw `runtime-state.json` 未纳入，Claim 已降级 |
| RA-PROC-006 | 命令目录契约 | `--output` 必须传父输出目录，Harness 自动创建 `process-chaos-<seed>`；不得把 seed 子目录再次传入造成双层同名目录 | SOP 已纠正；历史双层目录属于命令误用，不是 Runtime 缺陷 |
| RA-PROC-007 | 科研口径 | 只允许表述为 Team Workflow Return 一个固定 seed 的窄范围 E3 Process Check，科研矩阵 **1/40**；必须同时写明 GATE-40 未完成、不是完整 E3、不是 exactly-once、不是生产可用 | 强制门禁，任何过度表述均 No-Go |

### 17.3 Runtime-E2E 确定性门禁

| ID | 验收项 | 必须断言 | 当前证据/状态 |
|---|---|---|---|
| RA-RTE-001 | 错误路径规范化 | 只在异常记录源头把本次 `mkdtemp` 的 `caseDirectory`（Windows 原生分隔符和正斜杠形式）替换为 `<case-directory>`；不得删除整个 `recoveryResult` | RRA-04 已实现并通过直接测试 |
| RA-RTE-002 | 失败语义保留 | 规范化后仍保留 `snapshot_conflict`/`SnapshotConflict`、expected/found generation、相对 `runtime-state.json`、`taskSuccess=false`、Failure、Unknown Outcome、Evidence、重复副作用及全部安全/恢复指标 | RRA-04 回归断言已通过 |
| RA-RTE-003 | 固定失败用例多次回归 | 对 `no-lease` 的 `return-parent-feedback-001` 固定失败用例连续构建三次；移除墙钟字段后的确定性投影必须完全一致，同时墙钟字段仍存在 | Runtime-E2E 专项已覆盖 |
| RA-RTE-004 | GATE-30 多次确定性复跑 | 员工 D 的 GATE-30 通过；总控连续两次 GATE-30 均退出 0 且 `deterministicOutcomesVerified=true`；员工 C 再独立执行一次通过。任何一次新失败都必须保留日志并重新打开门禁 | 当前参考环境已满足；首次和后续失败仍作为已修复负结果保留 |

### 17.4 当前自动化回归账本

| 检查 | 当前观察结果 | 证据解释 |
|---|---:|---|
| 主测试 | 总控首次 482 tests / 481 pass / 1 fail；立即复跑 482/482；员工随后顺序三轮均 exit 0、482/482 | 首失败详情因工具输出截断丢失；三轮耗时 20.989/21.327/23.675 秒，完整 TAP 无 `not ok`/`error`/`stack`，Process #387 三轮均 `ok` 且无残留。本轮重复样本 0/3 失败，但 NEG-011 未定位、未修复，不自动升级为稳定或 E3 |
| Electron | 74/74 | Electron/子进程相关专项通过；不等于完整真机、容量或生产验收 |
| Lease | 21/21 | Lease/Fencing 专项 E2；不等于 Dynamic 全边界 Process E3 |
| Manifest | 9/9 | create/verify、正文绝对路径/GitHub Token 等负例入口通过；不等于最终 Artifact 已冻结 |
| Process Chaos | 独立验收 1/2 后立即复跑 2/2；修复后员工四轮及总控单轮 2/2 | Frozen/Clean 语义投影相同；范围仍为 1/40 |
| Runtime-E2E | 10/10 | 含错误路径规范化和多次确定性回归；Provider/Tool 仍为 Fake |

### 17.5 Artifact、Claim 与发布门禁

| ID | 门禁 | Go 条件 | 当前裁决 |
|---|---|---|---|
| RA-ART-001 | 原始文件 | Model、Runtime-E2E 保存本次新运行的报告、CSV、失败 repro、日志、环境记录和人工介入记录；Process 公开包保存报告、Lease、环境、命令与限制。旧摘要对应原始 JSON/CSV/repro 缺失的事实不得覆盖或伪装补回 | 已完成三层 Frozen Artifact；旧摘要仍只作为历史记录 |
| RA-ART-002 | 哈希与 Manifest | 对实际纳入文件计算 SHA-256，绑定新的运行 ID；三层分别 `create`、立即 `verify`，再在干净只读复制包复核；不得把新文件回填成旧运行原始文件 | 已完成；三个 Manifest 均绑定 baseline `05680a4...9af5b` |
| RA-ART-003 | Process 敏感路径处置 | v0.1 公开包排除 raw `runtime-state.json`，只纳入安全报告、Lease、环境、命令与限制；记录可重建方式和 Claim 降级，不得直接发布含本机绝对计划路径的原始状态 | 已完成；Process Manifest 3 files，只含 report、leases、SCOPE |
| RA-ART-004 | 同机干净源码副本复现 | 从无旧 results 载荷的 baseline 源码复制开始，仅按 SOP 执行；记录版本、命令、seed、耗时、退出码、哈希、Provider=Fake/none、失败和人工步骤 | 已完成；Model hash、Runtime projection、Process semantic projection 均与 Frozen 对齐；仍不是外部第三方/E4 |
| RA-ART-005 | Claim 边界 | 每个 Claim 必须回指机制、测试、原始 Artifact、证据等级和限制；保留失败、`outcome_unknown`、1/40、Fake Provider、本地单文件 CAS、旧原始文件缺失和外部有效性限制 | 本轮最终审稿已完成；NEG-011 与 UTC 日期转换负结果保留 |
| RA-ART-006 | Provider | 全部本轮门禁真实 Provider 请求数必须为 0，凭据读取必须为 false；依赖下载网络与本机 loopback Fake Server须另行说明 | 已完成；`npm ci` 下载 101 packages 单独记录，不是 Provider 调用 |
| RA-ART-007 | 两阶段 Commit 与 PR | G0/G1 和代码/文档审查通过后允许总控创建 baseline Commit；已披露且未复现的未知 flake 可作为显式风险进入 baseline，但不得删除或写成已修复。以 baseline SHA 完成 Artifact、同机干净副本复现、Manifest 和 Claim 门禁后，再创建 artifact Commit、Push 并更新 PR | baseline/Artifact/干净副本均已完成，本提交即 artifact Commit；Push/PR 说明必须披露 1/40、NEG-011、非第三方及 UTC 转换负结果 |

当前结论：baseline、三层 Frozen Artifact、Manifest verify 和同机干净源码副本复现已完成，本提交即 artifact Commit；Push/PR 状态以外部页面为准。NEG-011 仍未定位、未修复；外部第三方复现未完成；Process Chaos 仍只有 1/40，GATE-40 未完成。Clean Process Manifest 的 UTC 日期转换失败已记录为操作脚本负结果。以上完成状态不得写成生产可用、跨环境稳定、完整 E3 或论文闭环。

## 18. 双链科研版 Runtime v1.0 验收体系（RT95，2026-08-20）

完整需求、架构和小需求定义见 [D11](11-God-Agent双链科研版Runtime-95%完整度实施与验收计划.md)。本节是 RT95 的权威测试与 Go/No-Go 入口。

### 18.1 计分规则

| 状态 | 得分 | 必要条件 |
|---|---:|---|
| Verified | 1 | 原子 WP 的生产接线、全部规定 TC、原始证据和文档通过 |
| 其他任何状态 | 0 | Proposed、Accepted、In Progress、Implemented、Partial、Failed、Unknown 均不得得分 |

总分按 D01 第 12.1 节的 100 个原子 Work Package 计算。测试数量和代码行数不进入完成度公式。总分 ≥95/100 只是必要条件，P0 硬门禁、领域最低通过数和科研独立子账本必须同时通过。

### 18.2 P0 硬门禁

| Gate | 必须证明 | No-Go 条件 |
|---|---|---|
| RT95-G-P0-01 | 同一 Thread 默认最多一个可推进 Turn，不同 Thread 保持并行 | 竞态下出现双活或上下文顺序依赖时序 |
| RT95-G-P0-02 | Job Lease、CAS 和持久化分区粒度一致 | 不同 Job 合法 Owner 仍争抢全局 Snapshot generation |
| RT95-G-P0-03 | Plan Draft 经校验和原子 Commit 后才执行 | 未冻结 Task Contract/Graph 即启动 Child 或 Tool |
| RT95-G-P0-04 | Supervisor/Scheduler 从持久事实恢复 | 重启依赖已丢失的内存 Queue 或人工猜测 |
| RT95-G-P0-05 | Reviewer 读取 Artifact/Test/Oracle Proof | Summary 单独满足 acceptance criterion |
| RT95-G-P0-06 | Requirement 每一 criterion 进入 Completion Proof | 零 Task、漏拆或证据过期仍可 completed |
| RT95-G-P0-07 | Recovery 按副作用等级执行 | non-replayable 副作用被自动盲重试 |
| RT95-G-P0-08 | Chat/Job 的取消、结果、权限、凭据和状态隔离 | 任一跨 Job 串线或越权 |
| RT95-G-P0-09 | Dynamic 双 App Server 关键 E3 矩阵通过 | 仅组件测试或固定 Team 1/40 代替 Dynamic E3 |
| RT95-G-P0-10 | 外部复现者只按 SOP 重建三层证据 | 需要开发者口头指导或关键输入未归档 |

### 18.3 分层门禁

#### G0：合同与 ADR

- 需求 ID、Requirement revision、权重、非目标和验收已冻结；
- 状态迁移、故障模型、迁移和回滚策略明确；
- 未通过 G0 不得进入编码。

#### G1：组件 E2

- 类型检查、单元和集成测试通过；
- 正向、反向、重复、乱序、取消、超时、并发和恢复用例齐全；
- 所有新状态都有序列化、迁移和恢复测试。

#### G2：生产接线 E2

- 测试必须穿过 App Server 的实际组装入口；
- 组件不能只在 Test Double、Harness 或可选未注入依赖中存在；
- Fake Provider/Tool 必须在报告中明确标注。

#### G3：真实进程 E3

- 真实启动、强杀和重启两个 App Server/Worker；
- 覆盖预注册 commit boundary、Lease 丢失、迟到结果、Return、Receipt 和 Proof commit；
- 验证无残留进程、端口、错投结果和隐式重复副作用；
- 完整 stdout/stderr、环境和 Artifact 进入证据包。

#### G4：科研复现 E4 候选

- Frozen Artifact 和顶层 Manifest；
- 外部无指导复现；
- 完整消融、重复实验、置信区间和效应量；
- Claim–Evidence Matrix、失败和限制同步。

### 18.4 核心不变量攻击矩阵

| ID | 不变量 | 最少攻击方式 |
|---|---|---|
| INV-RT95-01 | 同 Thread 单活 Turn | 1000 次同时 start/run/cancel |
| INV-RT95-02 | 只有当前 fencing owner 可提交 | 双 App Server、Lease 过期、旧 Owner 迟到提交 |
| INV-RT95-03 | 已取消/失权 Run 不推进状态 | Model/Tool 忽略 Abort 后迟到 |
| INV-RT95-04 | 未 Commit Plan 不执行 | Validate/Commit 边界强杀 |
| INV-RT95-05 | 未满足依赖 Task 不 runnable | 乱序/重复 dependency event |
| INV-RT95-06 | Capability 只能收窄 | Child 提权、跨 Job Token/Path/MCP 注入 |
| INV-RT95-07 | Evidence 绑定 criterion/revision/digest | 旧 Evidence、替换 Artifact、错 revision |
| INV-RT95-08 | Summary 不构成完成证据 | Worker 自报成功但文件/测试失败 |
| INV-RT95-09 | non-replayable 不盲重放 | 提交后响应前强杀且外部不可查询 |
| INV-RT95-10 | Proof 不完整不能 completed | 零 Task、漏 criterion、blocking contradiction |
| INV-RT95-11 | Final Delivery 只有一个本地 Receipt | claim/commit/consume 各边界强杀和重放 |
| INV-RT95-12 | 跨 Job 完整隔离 | 3 Chat 并发取消、配额耗尽、错投 Return/Tool Result |
| INV-RT95-13 | 恢复仅依赖持久事实 | 丢弃全部内存队列后重启 |
| INV-RT95-14 | Claim 可追溯 | 从结论反查 commit、命令、原始报告和限制 |

### 18.5 科研实验门禁

消融至少包含 `full`、`no-model-wal`、`no-tool-wal`、`no-lease`、`no-receipt`、`no-supervisor-recovery`、`no-completion-proof`、`no-human-resolution`。指标必须区分 safety handling success、business completion、outcome known/unknown 和 duplicate external effect。

Dynamic 双 App Server E3 通过后，才允许执行完整 8 窗口 × 5 seed 的 GATE-40。报告至少包含失败率、恢复率、误完成率、重复副作用率、P50/P95 恢复时间、95% 置信区间和效应量。成功和失败原始结果必须一并冻结。

### 18.6 外部复现门禁

复现者必须是未参与开发的人，在另一台机器或新 VM 中仅依据仓库、baseline SHA 和 SOP 执行。证据必须记录复核者、源码/lockfile SHA、Node/npm/OS、逐条命令、时间、退出码、完整输出、投影 SHA、人工介入和签名；顶层说明、Clean Reproduction、失败日志和投影文件全部进入顶层 Manifest。

### 18.7 95% 最终 Go/No-Go

只有同时满足以下条件才允许 Go：

1. 加权总分 ≥95%；
2. 十项 P0 Gate 全过；
3. D01–D04 和 D11 同步；
4. 全量测试无回归；
5. Dynamic 双 App Server E3 与 GATE-40 完成；
6. 外部无指导复现完成；
7. 所有负结果进入 D04 和 Artifact；
8. 对外 Claim 未超过证据等级。

当前裁决：`No-Go / Proposed`。现有 501/501、三层 Manifest 和同机 Clean Reproduction 是重要基线，但不能满足 RT95-G-P0-03～10，也不能据此把任何 RT95 阶段写成 Verified。

## 19. TC-RT95 预注册测试账本（150 条，v0.1）

本节的 150 条均为 **测试合同**，不是已经实现或通过的测试。需求/ADR 尚未冻结的条目标记为 `Designed/Blocked by Decision`；只有生产入口、规定重复次数、Oracle、原始证据和文档全部成立，才能改为 Verified。

当前账本状态：`Designed=150`、`Implemented=0`、`Verified=0`。既有测试可以作为未来用例的证据候选，但必须完成 ID 映射、生产入口核验和规定 Oracle 后才能迁移状态，禁止按名称相似自动认领通过。

### 19.1 每条用例的强制记录字段

后续应将本账本同步为机器可读 Ledger，每条至少包含：

```yaml
id:
requirementIds:
workPackageIds:
priority:
preconditions:
steps:
faultBoundary:
oracle:
invariants:
expectedEvidenceLevel:
evidenceFiles:
seeds:
repeatCount:
currentStatus:
blockedByDecision:
limitations:
```

证据代码：`S`=Schema/ADR/静态接线，`U`=自动测试/快照/调用计数，`R`=生产 RPC/权威状态，`X`=PID/kill/Lease/Journal/WAL，`A`=Artifact/Manifest/哈希/环境，`M`=性能/资源/分位数。

不变量代码沿用第 18.4 节 `INV-RT95-01～14`。表中 `P0=是` 表示该用例失败会阻断对应阶段；并非所有 P0 都要求每个小 PR 执行，分层执行见第 19.18 节。

### 19.2 正常主链：TC-RT95-001～010

前置：Requirement、Turn 策略、基本 Grant 已冻结；Fake Provider 明示。证据：`U/R`。主要不变量：01、05、10、12。

| ID | 步骤/场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-001 | 单 Chat 正常 Turn | 唯一 completed Turn、唯一 Assistant | E2/E3 | 是 |
| TC-RT95-002 | 三 Chat 同时执行 | 三套 Context、状态、结果零交叉 | E3 | 是 |
| TC-RT95-003 | Model→Tool→Model | WAL 顺序正确，最终结果一次 | E2/E3 | 否 |
| TC-RT95-004 | 父提交三个独立 Task | 三 Child 并行并分别产生 Return | E3 | 是 |
| TC-RT95-005 | fork/join DAG | Join 只在所有上游验证后启动 | E2/E3 | 是 |
| TC-RT95-006 | Child 先后完成 | 先完成者立即验证并解锁下游 | E3 | 是 |
| TC-RT95-007 | Reviewer 正常通过 | Proof Fragment 被引用，Task completed | E2/E3 | 是 |
| TC-RT95-008 | Supervisor 等待人工反馈 | 按原 Job/Plan revision 恢复 | E3 | 是 |
| TC-RT95-009 | 全部 criterion 有效覆盖 | Completion Proof 唯一提交 | E3 | 是 |
| TC-RT95-010 | Final Delivery | 唯一可见结果和唯一 Receipt | E3 | 是 |

### 19.3 反向与 Fail-closed：TC-RT95-011～020

前置：Contract/Graph/Proof Schema 冻结。证据：`U/R/S`。主要不变量：04～10。

| ID | 注入 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-011 | 缺 objective | Plan 拒绝，零 Child/副作用 | E2 | 否 |
| TC-RT95-012 | 缺 acceptanceCriteria | Plan 拒绝 | E2 | 是 |
| TC-RT95-013 | criterion 未映射 Task/Oracle | Coverage Gate 拒绝 | E2 | 是 |
| TC-RT95-014 | DAG 引用不存在 Task | Graph Commit 拒绝 | E2 | 否 |
| TC-RT95-015 | DAG 有环 | Graph Commit 拒绝 | E2 | 是 |
| TC-RT95-016 | Child 请求扩大权限 | fail-closed，Effective Grant 不变 | E2/E3 | 是 |
| TC-RT95-017 | Artifact 路径越界 | 验证前拒绝，不读取边界外文件 | E2/E3 | 是 |
| TC-RT95-018 | 只提交 Worker Summary | criterion 保持未覆盖 | E2 | 是 |
| TC-RT95-019 | Proof 含 blocking contradiction | Job 不得 completed | E2/E3 | 是 |
| TC-RT95-020 | 不可查询副作用无结果 | outcome_unknown，禁止自动成功 | E3 | 是 |

### 19.4 重复、乱序和迟到事件：TC-RT95-021～030

前置：Event Envelope、幂等键和 Reducer 冻结。证据：`U/R`。主要不变量：02、03、07、11～13。

| ID | 注入 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-021 | 同 eventId 重放 100 次 | 状态只推进一次 | E2 | 是 |
| TC-RT95-022 | 同业务事件不同 eventId | 业务幂等键阻止重复副作用 | E2 | 是 |
| TC-RT95-023 | completed 先于 started | 按冻结策略缓冲或拒绝 | E2 | 否 |
| TC-RT95-024 | Evidence 先于 Task claim | 不误完成，事件可解释 | E2 | 否 |
| TC-RT95-025 | Return 重复 ready/delivering | Receipt 唯一 | E2/E3 | 是 |
| TC-RT95-026 | consumed 后迟到 ready | 终态不倒退 | E2 | 是 |
| TC-RT95-027 | 旧 Plan revision 事件迟到 | 不推进新 Graph | E2/E3 | 是 |
| TC-RT95-028 | 旧 Run heartbeat 迟到 | 不续租新 Run | E2 | 是 |
| TC-RT95-029 | Job A 事件携带 Job B Task | 拒绝并审计错投 | E2/E3 | 是 |
| TC-RT95-030 | Proof 后迟到失败摘要 | Proof 不倒退，矛盾进入审计 | E2 | 否 |

### 19.5 并发竞态：TC-RT95-031～040

前置：确定性 Barrier/Seed；除特别说明外重复 1000 轮。证据：`U/R/X`。主要不变量：01～03、11、12。

| ID | 竞态 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-031 | 同 Thread 两个 start | 1000 轮零双活 | E2/E3 | 是 |
| TC-RT95-032 | Turn complete 与 cancel | 唯一合法终态 | E2/E3 | 是 |
| TC-RT95-033 | Turn timeout 与 Model 返回 | 迟到结果不发布 | E2/E3 | 是 |
| TC-RT95-034 | Job cancel 与 Child complete | Job 不复活 | E2/E3 | 是 |
| TC-RT95-035 | 两 Child claim 同 Task | 唯一 Owner | E2/E3 | 是 |
| TC-RT95-036 | 两 Task claim 重叠路径 | 至多一个 runnable/running | E2 | 是 |
| TC-RT95-037 | 两 Reviewer 同时裁决 | 一份有效 verdict 或显式冲突 | E2 | 否 |
| TC-RT95-038 | Proof commit 与 Amendment | revision 一致，不混合证据 | E2/E3 | 是 |
| TC-RT95-039 | 两人工 resolve unknown | 单版本成功 | E2/E3 | 是 |
| TC-RT95-040 | Job A/B 同时大量 Return | 不错投、不饥饿、不丢失 | E3 | 是 |

### 19.6 取消、超时和迟到结果：TC-RT95-041～050

前置：可控 Provider/Tool/Worker Barrier。证据：`U/R/X`。主要不变量：03、09、11、12。

| ID | 注入边界 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-041 | Model 请求中取消 Turn | interrupted，响应只进入审计 | E2/E3 | 是 |
| TC-RT95-042 | Tool 执行中取消 | 只取消所属 Invocation | E2/E3 | 是 |
| TC-RT95-043 | 忽略 Abort 的 Model 迟到 | 不生成 Assistant/Continuation | E2/E3 | 是 |
| TC-RT95-044 | 忽略 Abort 的 Tool 迟到 | result_received/unknown，不提交业务 | E2/E3 | 是 |
| TC-RT95-045 | Child deadline | timed_out Return，父继续裁决 | E2/E3 | 否 |
| TC-RT95-046 | Supervisor wait deadline | 执行冻结的 timeoutAction | E2/E3 | 是 |
| TC-RT95-047 | Guidance 过期后到达 | 不进入 Child Context | E2 | 是 |
| TC-RT95-048 | 人工回复对应旧 Plan | 拒绝或要求 Amendment | E2/E3 | 是 |
| TC-RT95-049 | Job A cancel、Job B Tool 活动 | B 不收到 Abort | E3 | 是 |
| TC-RT95-050 | Final Delivery 后 cancel | 可见结果与 Receipt 不倒退 | E3 | 是 |

### 19.7 崩溃恢复：TC-RT95-051～060

前置：真实子进程、真实文件、公共 RPC；故障点落盘确认后强杀。证据：`X/A`。主要不变量：02、03、09、11、13。

| ID | 强杀边界 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-051 | Plan Draft 写后 | 未 Commit Plan 不调 Child | E3 | 是 |
| TC-RT95-052 | Plan Commit 后 Dispatch 前 | 从持久 Graph 恢复 Queue | E3 | 是 |
| TC-RT95-053 | Model submitted 后 | unknown/查询，禁止盲重发 | E3 | 是 |
| TC-RT95-054 | Model response_received 后 | 零 Provider 重放，只补提交 | E3 | 是 |
| TC-RT95-055 | Tool executing 中 | 按 side-effect class 恢复 | E3 | 是 |
| TC-RT95-056 | Tool result_received 后 | 零 Tool 重放，只补提交 | E3 | 是 |
| TC-RT95-057 | Return claim 后 | Receipt 唯一，不丢 Return | E3 | 是 |
| TC-RT95-058 | Parent continuation 后 consume 前 | 不重复父模型，只补 consume | E3 | 是 |
| TC-RT95-059 | Proof commit 后 Delivery 前 | 从 Proof 恢复一次交付 | E3 | 是 |
| TC-RT95-060 | Delivery 后 Receipt 前 | 本地可见交付保持唯一 | E3 | 是 |

### 19.8 双 App Server / Worker：TC-RT95-061～070

前置：两个真实 App Server/Worker，共享受控状态目录。证据：`X`。主要不变量：02、03、11～13。

| ID | 场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-061 | 两 Server 同时恢复同 Job | 一个 Owner，一个 waiting | E3 | 是 |
| TC-RT95-062 | 两 Server 分别执行 Job A/B | 并行提交，无全局冲突 | E3 | 是 |
| TC-RT95-063 | Owner 暂停超 TTL 后 takeover | 新 Token 唯一有效 | E3 | 是 |
| TC-RT95-064 | 旧 Owner 恢复后提交 | 100% fenced | E3 | 是 |
| TC-RT95-065 | Worker 强杀 | Supervisor 有界发现失联 | E3 | 是 |
| TC-RT95-066 | Reviewer 强杀 | Review 恢复，Worker 事实不改写 | E3 | 是 |
| TC-RT95-067 | Supervisor 强杀 | 从 Journal 继续，不猜内存 | E3 | 是 |
| TC-RT95-068 | 两 Worker 竞争同 Task | 单 Claim/Attempt | E3 | 是 |
| TC-RT95-069 | MCP Worker/子进程崩溃 | 只影响所属 Invocation | E3 | 否 |
| TC-RT95-070 | 重启后资源检查 | 无残留进程、端口、锁 | E3 | 否 |

### 19.9 Lease / Fencing：TC-RT95-071～080

前置：可控时钟；Lease TTL 与锁陈旧判定分离。证据：`U/X`。主要不变量：02、03。

| ID | 场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-071 | 1000 次同 Job acquire race | 每轮唯一 Owner | E2/E3 | 是 |
| TC-RT95-072 | Renew | version 增加，Token 不变 | E2 | 否 |
| TC-RT95-073 | Takeover | fencing Token 单调增加 | E2/E3 | 是 |
| TC-RT95-074 | Expired Owner commit | 即使未 takeover 也拒绝 | E2/E3 | 是 |
| TC-RT95-075 | Return 边界旧 Token | 拒绝 | E3 | 是 |
| TC-RT95-076 | Model/Tool 边界旧 Token | 拒绝并保留 unknown | E3 | 是 |
| TC-RT95-077 | Proof 边界旧 Token | Job 不 completed | E3 | 是 |
| TC-RT95-078 | Cancel 边界旧 Token | 不覆盖新 Owner 状态 | E3 | 是 |
| TC-RT95-079 | 同 Job 嵌套调用 | 复用本机 Session，不死锁 | E2/E3 | 否 |
| TC-RT95-080 | Job A 内嵌套 Job B | fail-closed，A 不污染 B | E2 | 否 |

### 19.10 Job 分区与 Snapshot v7 迁移：TC-RT95-081～090

前置：目录布局、Manifest、Migration Marker 和回滚 ADR 冻结。证据：`U/R/X/A`。主要不变量：02、12～14。

| ID | 场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-081 | 只读加载 v7 | 语义完整且不自动迁移 | E2 | 是 |
| TC-RT95-082 | v7→分区迁移 | aggregate 数量/哈希匹配 | E2/E3 | 是 |
| TC-RT95-083 | global 写后迁移中断 | 重启选择唯一一致版本 | E3 | 是 |
| TC-RT95-084 | 部分 Job 迁移后中断 | 不混合读取新旧权威 | E3 | 是 |
| TC-RT95-085 | 重复执行迁移 | 幂等，无重复 Job/Event | E2/E3 | 是 |
| TC-RT95-086 | Job A 分区损坏 | B 可读，A 隔离恢复 | E3 | 是 |
| TC-RT95-087 | Job ID 路径逃逸 | 迁移/打开拒绝 | E2 | 是 |
| TC-RT95-088 | 分区 generation 冲突 | 只影响所属 Job | E3 | 是 |
| TC-RT95-089 | 回滚兼容读取 | 不静默丢失新语义 | E2/E3 | 否 |
| TC-RT95-090 | Migration Artifact Manifest | 文件、哈希、版本链可验证 | E4 | 否 |

### 19.11 Task Contract / Graph：TC-RT95-091～100

前置：Task Contract、Task ID 和 Plan version 冻结。证据：`U/R`。主要不变量：04～07。

| ID | 场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-091 | 完整 Contract 正常提交 | 字段无默认降级 | E2/E3 | 是 |
| TC-RT95-092 | Draft 未 Commit 即 Dispatch | 拒绝，零副作用 | E2/E3 | 是 |
| TC-RT95-093 | 原子 Commit Graph | 不出现半图 | E2/E3 | 是 |
| TC-RT95-094 | Task ID 稳定重放 | 同 Plan 得相同 ID | E2 | 否 |
| TC-RT95-095 | acceptance coverage 完整 | 每 criterion 至少一 Task/Oracle | E2 | 是 |
| TC-RT95-096 | File Claim 冲突 | Reject 或显式串行化 | E2 | 否 |
| TC-RT95-097 | Task 预算总和超 Job | Commit 前拒绝 | E2 | 是 |
| TC-RT95-098 | Amendment 增加 Task | 新 revision、Diff、原因持久化 | E2/E3 | 是 |
| TC-RT95-099 | 删除已有副作用 Task | 拒绝静默删除，保留事实 | E2/E3 | 是 |
| TC-RT95-100 | 旧 Graph 事件到新 Graph | 不解锁、不完成新节点 | E2/E3 | 是 |

### 19.12 Supervisor / Wake / Guidance / Heartbeat：TC-RT95-101～110

前置：Supervisor、WaitSpec、Mailbox 和 Heartbeat 协议冻结。证据：`U/R/X`。主要不变量：03、05、13。

| ID | 场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-101 | Supervisor 全状态正向迁移 | 每步有持久事件/前置条件 | E2/E3 | 是 |
| TC-RT95-102 | 依赖完成 Wake | 下游立即解锁，无忙轮询 | E2/E3 | 是 |
| TC-RT95-103 | 任一 Child 完成 | 单独验证，不等整批 | E3 | 是 |
| TC-RT95-104 | 人工回复 Wake | 正确 Job/WaitSpec 消费一次 | E3 | 是 |
| TC-RT95-105 | Quota 恢复 Wake | waiting→runnable 一次 | E2/E3 | 否 |
| TC-RT95-106 | Guidance 安全边界消费 | 不打断执行中的 Model/Tool | E2/E3 | 是 |
| TC-RT95-107 | Guidance 重复/错 Run | fail-closed，Receipt 唯一 | E2 | 是 |
| TC-RT95-108 | 正常 Heartbeat | Lease 延续，无 lost 误报 | E2/E3 | 否 |
| TC-RT95-109 | Worker 停止 Heartbeat | 有界进入 lost/recovery decision | E3 | 是 |
| TC-RT95-110 | Supervisor 重启 | Wait/priority/deadline/reason 一致 | E3 | 是 |

### 19.13 Evidence / Proof / Arbiter：TC-RT95-111～120

前置：Evidence、Proof Fragment、CompletionProof、Contradiction、Arbiter Schema 冻结。证据：`U/R/X/A`。主要不变量：07～11。

| ID | 场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-111 | 文件存在且 digest 正确 | Artifact Verifier passed | E2/E3 | 是 |
| TC-RT95-112 | 文件替换/digest 错 | criterion 未满足 | E2/E3 | 是 |
| TC-RT95-113 | 测试命令成功 | 绑定命令、exit、report hash | E2/E3 | 是 |
| TC-RT95-114 | 报告过期/旧 Plan | Evidence rejected stale | E2 | 是 |
| TC-RT95-115 | 外部 Oracle 不一致 | blocking contradiction | E2/E3 | 是 |
| TC-RT95-116 | Reviewer 与 Worker 隔离 | Reviewer 只读 Proof/Artifact | E3 | 是 |
| TC-RT95-117 | 两 Reviewer 冲突 | 创建 Arbiter，不多数投票 | E2/E3 | 是 |
| TC-RT95-118 | Arbiter 不引用冲突证据 | Verdict 拒绝 | E2 | 是 |
| TC-RT95-119 | Proof 缺一 criterion | Job 不 completed | E2/E3 | 是 |
| TC-RT95-120 | Proof 完整无矛盾 | 单 generation 提交并允许交付 | E3 | 是 |

### 19.14 Capability / Namespace：TC-RT95-121～130

前置：Effective Grant 与 Namespace 标识冻结。证据：`U/R/X`。主要不变量：06、12。

| ID | 攻击/场景 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-121 | Profile 允许、Job 禁 Tool | 最终拒绝 | E2/E3 | 是 |
| TC-RT95-122 | Job 允许、Task 禁 Tool | 最终拒绝 | E2/E3 | 是 |
| TC-RT95-123 | 用户未确认写操作 | 副作用前拒绝 | E3 | 是 |
| TC-RT95-124 | Job A 读取 Job B Artifact | Namespace 拒绝 | E3 | 是 |
| TC-RT95-125 | Job A 使用 Job B Credential | 拒绝且日志不泄密 | E3 | 是 |
| TC-RT95-126 | MCP Result 错 correlationId | 不交给错误 Invocation | E2/E3 | 是 |
| TC-RT95-127 | Tool Result 错投 Turn/Job | Reducer 拒绝 | E2/E3 | 是 |
| TC-RT95-128 | Child 请求 full_access | 只能取交集，不能扩大 | E2/E3 | 是 |
| TC-RT95-129 | Worktree 链接/路径逃逸 | 拒绝 | E2/E3 | 是 |
| TC-RT95-130 | Terminal 跨 Job 控制 | 不能 signal/attach 他 Job 进程 | E3 | 是 |

### 19.15 Quota / 背压 / 公平：TC-RT95-131～140

前置：配额单位、阈值、等待和失败策略冻结。证据：`U/R/M`。主要不变量：12、13。

| ID | 压力 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-131 | Job A Token 耗尽 | A 等待/失败，B 继续 | E2/E3 | 是 |
| TC-RT95-132 | Tool 并发超限 | 有界排队，无超发 | E2/E3 | 否 |
| TC-RT95-133 | 进程数超限 | 新进程不启动，旧进程不误杀 | E3 | 是 |
| TC-RT95-134 | Tool 输出字节超限 | 按合同截断/失败，无内存失控 | E2/E3 | 否 |
| TC-RT95-135 | Artifact 磁盘超限 | 原子拒绝，不写半文件 | E2/E3 | 是 |
| TC-RT95-136 | A=100 Child、B=1 Child | B 等待时间有界 | E2/E3 | 是 |
| TC-RT95-137 | Quota Wait 时重启 | 原等待原因/优先级恢复 | E3 | 是 |
| TC-RT95-138 | deadline 早于排队机会 | 明确 timeout，不静默丢 Task | E2/E3 | 否 |
| TC-RT95-139 | Quota 计数重复事件 | 不重复扣减/返还 | E2 | 是 |
| TC-RT95-140 | MCP/Tool Storm | 其他 Job 延迟/正确性过门禁 | E3 | 是 |

### 19.16 性能、耐久与复现：TC-RT95-141～150

前置：硬件、Commit、配置、Provider 类型和阈值冻结。证据：`M/X/A`。主要不变量：12～14。

| ID | 工作负载 | Oracle | 等级 | P0 |
|---|---|---|---|---|
| TC-RT95-141 | 目标并发 Spike 5 分钟 | 无状态污染/失控拒绝 | E3 | 否 |
| TC-RT95-142 | Steady 30 分钟 | 吞吐/P95/错误率达阈值 | E3 | 否 |
| TC-RT95-143 | 70% 并发 2 小时 | RSS/句柄/Queue 无持续泄漏 | E3 | 否 |
| TC-RT95-144 | 8 小时耐久 | 无累积重复 Return/Proof | E3 | 否 |
| TC-RT95-145 | 1k/10k/100k 对象 | 查询/恢复不超冻结阈值 | E3 | 否 |
| TC-RT95-146 | Kill 后 Recovery Drain | 积压清空且无重复副作用 | E3 | 是 |
| TC-RT95-147 | GATE-40：8 窗口×5 seed | 保存全部正负 raw | E3 | 是 |
| TC-RT95-148 | 完整单变量消融 | 每次仅移除一个机制 | E4 候选 | 否 |
| TC-RT95-149 | 外部无指导复现 | 两个环境按 SOP 得到规定投影 | E4 候选 | 是 |
| TC-RT95-150 | Manifest/Claim 门禁 | Claim、Commit、报告、限制一致 | E4 候选 | 是 |

### 19.17 P0 Process Chaos 预注册笛卡尔矩阵

完整 Runtime Chaos 不能继续只用 Team Return 的 `1/40` 代替。目标矩阵至少覆盖：

```text
6 类执行对象 × 8 类关键边界 × 5 个固定 seed = 240 个 E3 运行单元
```

执行对象：Model Invocation、Tool Invocation、Task/Worker、Return/Parent Continuation、Proof/Final Delivery、Partition Migration。

关键边界：

1. prepare 前；
2. prepare 后/dispatch 前；
3. dispatch 后/result 前；
4. result 后/business commit 前；
5. business commit 后/receipt 前；
6. receipt 后/release 前；
7. takeover 后旧 Owner 迟到；
8. recovery decision 持久化后/继续前。

其中 GATE-40 是预先选定的核心 8 窗口 × 5 seed 功能覆盖入口，不替代完整 240 单元，也不替代统计重复实验。

### 19.18 分层执行策略

| 门禁 | 用例范围 | 执行时机 |
|---|---|---|
| P0-A 组件阻断 | 011～039、071～080、091～100、111～120 中 P0 | 每个相关 PR |
| P0-B 生产组装 | 001～010、041～050、101～140 中 P0 | 阶段集成 PR |
| P0-C 双实例/Chaos | 051～070、081～089、146 | 合并前、冻结前 |
| P0-D 科研冻结 | 147、149、150 及附录 C 科研门禁 | Artifact/正式实验版本 |

### 19.19 当前阻塞的设计决策

下列测试已经预注册，但在 ADR 冻结前不能写死可执行 Oracle：

1. 第二 Turn 采用 reject、queue 还是 cancel-and-replace；
2. Job 分区布局、Journal、Migration Marker 和回滚协议；
3. 乱序窗口、去重保留期和非法事件处理；
4. 稳定 Task ID 与 Amendment merge/replace 语义；
5. WaitSpec wake 类型、deadline 和是否允许 bounded poll；
6. Guidance 安全边界、版本比较和 Receipt；
7. Heartbeat interval、TTL、slow/blocked/lost 阈值；
8. Side-effect 分类声明主体和 query protocol；
9. Evidence 新鲜度、digest 算法和 criterion mapping；
10. Arbiter 触发阈值及其与确定性 Oracle 的权威关系；
11. CapabilityGrant、Credential Handle 和 MCP Namespace；
12. Quota 单位、默认值、软硬限额和等待/失败策略；
13. 性能参考机器和通过阈值；
14. 外部复现的人员独立性、环境差异和投影一致定义。

这些条目当前状态应为 `Designed/Blocked by Decision`，不能算 Implemented，更不能算 Verified。

## 20. W0 实测证据与机器门禁

### 20.1 计数口径

测试分母必须写成“150 个顶层 `TC-RT95-*` + 46 个非计分 W0 组件变体”，不得相加宣传为 196 个顶层测试，也不得用 46 个组件通过自动升级任一顶层 TC。

当前机器输出：

```text
engineering: 100 / Verified 0
tests: topLevelTC 150 / componentVariants 46 / topLevelVerified 0
research: 66 / Verified 0 / P0 0 of 18 / research95Eligible false
```

### 20.2 总控最终正向结果

| 门禁 | 结果 | 证据等级与限制 |
|---|---:|---|
| W0 合同专项 | 46/46 | C01～C03 28 + Capability/Evidence 18；组件 E2 |
| Requirement | 3/3 | Requirement revision、确认、Job 绑定 |
| 类型检查 | 通过 | 静态证据 |
| Snapshot 持久化专项 | 26/26 | 含两个真实 Windows delete-sharing 用例 |
| Runtime-E2E | 10/10，连续 3 轮 | 含窄范围真实 App Server；不等于完整 E3 |
| Lease | 21/21 | 含 1000 次竞态和独立进程 |
| Electron | 74/74 | 桌面组件/真实 App Server 子进程窄范围 |
| 全量主测试 | 最终 484/484 | 修复后新增 2 条 Windows 持久化用例 |
| Completion 分离反例 | 7/7 | `unexpectedInverseRejections=0` |
| KAT | 通过 | fixture `b59a972d...edf55bc`；SOP `ae7fb1bb...bf6fe`；BOM rejected |
| Validator 攻击 | 12/12 均被拒绝 | 每个攻击应非零退出 |

没有调用真实 Provider。上述结果不能替代 Dynamic 双 App Server 完整矩阵、GATE-40、外部副作用真实系统或生产容量实验。

### 20.3 Validator 必须拒绝的攻击

- 顶层 TC 使用 `Verified + evidenceFiles=[package.json]`；
- 分母改为 1、分类计数改为 999、默认状态改为 Verified；
- Variant 把 `scoringEffect` 改为顶层晋级、把 E2 Claim 改成生产可用；
- Research 状态策略、最低 63/66、外部基线 `020 OR 021` 被篡改；
- `ReviewerAccepted` 被计为 Verified；
- 打开 `verifiedStatusEnabled`；
- 伪造 63 个 Research Verified、覆盖全部 P0 和外部基线，仅引用 `package.json`。

当前 v1 缺少结构化 Test/Research Completion Proof，因此顶层 TC 和 Research 项的 `Verified` 均 fail-closed 禁止。Validator 结构通过不等于 95% 资格通过。

### 20.4 Windows `EPERM` 回归门禁

Snapshot 原子替换只允许在 Windows 且错误码为 `EPERM` 时，保持同一跨进程锁、同一临时文件和同一 generation，重试同一个 `rename`。总等待上限为 1000ms：

- 目标文件拒绝 delete sharing 250ms：同一次保存成功，generation 只增加一次；
- 目标文件持续拒绝 1500ms：重试耗尽后仍原样抛出 `EPERM`，磁盘和内存 generation 不推进；
- 非 Windows 或非 `EPERM` 错误不得进入该重试。

### 20.5 仍需保留的失败证据

- 修复前全量 481/482、独立 Runtime-E2E 9/10，连续两次均为 Windows `EPERM rename`；
- 修复后一次全量 483/484，但长输出截断导致失败身份未保留；立即筛选复跑为 484/484。该未知失败属于测试可观测性缺口；
- 三轮 Runtime-E2E 的 Process Chaos 子测试曾出现约 32.6s、33.5s、3.4s，另一轮也复现约 33.7s。固定约 30 秒阶跃更符合“强杀发生在 Snapshot lock 临界区，继任者等待默认 `staleLockMs=30000` 后回收死 owner”的解释，而非单次最多 1 秒的 rename 重试；但尚缺 rename retry、lock acquire wait 和 stale claim recovery 计数，当前只能作为强证据假设。后续应补诊断和分位数阈值；
- 历史 Windows `EPERM` 和本轮失败均不得被最终绿灯覆盖。

W0 测试结论：合同/账本/窄范围 Windows 修复可以接受；顶层 150 TC 仍是 0 Verified，完整 E3、GATE-40 和 E4 仍为 No-Go。

## 21. RT95 测试可执行性补强与分层样本合同（2026-08-20）

### 21.1 当前测试资产不等于顶层覆盖

本轮静态盘点得到 80 个 `tests/*` 文件和该目录 559 个行首 `test(` 声明；不含 `research/` 下 17 个相关声明。最近全量执行是 484/484。四个数字必须分开解释：

| 数字 | 含义 | 不能代表 |
|---:|---|---|
| 559 | 静态测试声明近似数 | 实际执行次数、科研样本量 |
| 484/484 | 某次主测试运行结果 | 150 个 RT95 顶层 TC 已覆盖 |
| 46 | W0 组件候选变体 | 顶层 Verified、生产接线或 E3 |
| 150 | 顶层验收合同分母 | 已经存在 150 个可执行 Runner |

当前只有 17/150 顶层 TC 挂候选变体；122 个 P0 中 106 个没有候选变体。问题不是继续增加标题，而是给顶层 TC 增加可执行、分层、可重复的 `caseVariant`。

### 21.2 `CaseVariantExecutionContract v1`

每个新变体分为预执行合同和运行后只追加事实，禁止提前填写实际 Evidence：

| 字段 | 要求 |
|---|---|
| 身份 | variantId、关联 WP、top-level TC、invariant、证据层级和 Claim Ceiling |
| 入口 | production entry point、assembly 类型、Provider/Tool 类型、平台和进程拓扑 |
| 输入 | Requirement/Plan revision、Fixture/config/fault digest、初始权威状态 |
| 调度 | PRNG algorithm/version、Seed set、repeat、deadline、kill/commit window、时钟策略；Barrier participants/reached predicate/release order/timeout |
| 执行 | working directory、environment allowlist/redaction、setup/test/cleanup 精确命令、expected exit/timeout；PID、端口、隔离目录生成规则 |
| Oracle | RPC 结果、权威状态、Event/Invocation、调用次数、Lease/Journal/Receipt、外部状态的联合断言 |
| 预期产物 | `expectedArtifactPaths`、output schema、oracle spec、reviewer role、Claim Ceiling；只描述将产生什么 |
| 运行后事实 | `actualEvidenceRefs`、rawManifestDigest、proofFragmentRef、reviewerVerdict、status；Designed 时必须空/null，只能追加 |
| 失败 | 即时停止条件、失败 Raw 保留、flake/排除规则、reopen/Claim 降级动作 |
| 计划审查 | executor role、independent reviewer role；只冻结角色，不预填 verdict |
| 实际审查 | proofFragmentRef、reviewerVerdict、reviewedAt、finalStatus；运行后只追加 |

测试文件名、模型 Summary、单一 exit code 或“没有抛异常”都不能单独作为顶层 Oracle。

Validator v2 只对 `CaseVariantExecutionContract v2` 和 W1+ 新变体执行上述规则：拒绝 Designed Variant 预填 actual Evidence，也拒绝 passed Variant 缺 Raw Manifest 或 Reviewer。46 个 W0 v1 Variant 继续保持 non-scoring legacy candidate；迁移到 v2 前不得晋级或计入顶层 TC，不能被 v2 规则误判为新证据。

### 21.3 各测试层最低执行合同

下表中的次数是工程默认下限，不是正式论文样本量。正式样本必须由 Pilot 和预注册样本量分析决定。

| 层级 | 真实入口与证据上限 | 最低样本 / Seed | 必须 Oracle | 即时停止条件 |
|---|---|---|---|---|
| Contract/Schema | Schema、Validator、ADR；S/E1 | 每字段 1 正向；缺失/错型/边界/额外字段/降级篡改各 1 | exact schema、digest、状态白名单、Claim Ceiling | Validator 接受任一禁止输入 |
| Unit/Model | 纯函数、Reducer、Codec；E2 component candidate | 每分支/非法转移；property/竞态 5 seed×200=1000 | 前后置条件、同 seed 确定性、最小反例 | 不变量违反或同 seed 输出漂移 |
| Integration | 真实 Store/WAL/Lease/Reducer，Fake 外部；E2 | 普通路径 fresh fixture 连续 3 次；重放 100；竞态 5×200 | State+Event+Invocation/调用计数联合断言 | 状态倒退、重复效果、错投、unknown 被成功化 |
| 单 App Process | 公共 RPC、真实 App Server、独立目录，Fake Provider；E2 production candidate | 每 P0 边界 5 seed×3 个 fresh PID=15 | RPC+Raw State+PID/清理；不能证明多实例 fencing | 残留 PID/端口/锁或 Raw 与报告矛盾 |
| 双 App E3 | 两个真实 App Server/Worker，共享受控状态 | ownership/cancel/partition 5×20=100；明确要求 1000 轮者仍 5×200 | 单 Owner、旧 token 全拒绝、跨 Job 独立、事实恢复 | 双活、旧 token 提交、混合权威、重复副作用 |
| Chaos 功能覆盖 | 真实 kill/restart；E3 | GATE-40=8窗口×5 seed；完整 6对象×8窗口×5=240 | 每窗口 commit/receipt/proof/unknown 专用 Oracle | 任一 Safety P0，停止放大但保存现场 |
| 正式 Chaos/消融 | R3 统计输入 | 每窗口≥50 paired seed 仅为规划下限。Pilot 是否进入正式分析、样本量方法、最大 n 和停止规则须在 EXP-006 预注册；confirmatory run 前冻结 n/digest，开始后不得按效应或显著性调整 | Full/Baseline/Ablation 同任务/Seed/Budget/Oracle/Fault | 偏离预注册、Instrumentation 丢事件、意外真实 Provider |
| Long-run/Capacity | E3 稳定性 | Spike 5m×3；Steady 30m×3；Soak 2h×3；Drain 5 seed×3；8h 至少 2 次 | 吞吐、P95/P99、RSS/heap/handle、queue drain 和安全不变量；冻结 observation window、最小操作数、error 分母、baseline manifest、GC policy、queue deadline | 阈值字段未冻结时只预警；冻结后才执行 error/P95/RSS/queue 机器判定 |
| External/Baseline | R4/E4 candidate | 至少 1 个可执行外部基线；R4 至少 2 人/2 环境 | 同 Projection/Manifest/Oracle；指导自动降级 guided | 关键步骤依赖作者口头补充或 Hash/环境不闭合 |

### 21.4 150 顶层 TC 的最低层级与重复规则

| TC 范围 | 最低执行要求 |
|---|---|
| 001～010 正常主链 | Integration；P0 再做单 App Process；涉及多实例/父子恢复者做 E3 |
| 011～020 Fail-closed | Contract+Unit+Integration；每种缺失、越权、unknown 至少 1 正 1 负 |
| 021～030 重复/乱序/迟到 | 重放 100；冻结顺序排列；TC-029 跨 Job 错投必须 E3 |
| 031～040 并发竞态 | E2 5×200=1000；跨 Server/Return burst 再做 E3 5×20 |
| 041～050 取消/迟到 | Barrier E2 5×200；跨 Job Cancel/Final Delivery 做 E3 5×20 |
| 051～060 崩溃恢复 | 每个 commit boundary 真实 kill×5 seed，全部 E3 |
| 061～070 双 App/Worker | 两真实 PID；每场景 5×20；TC-070 每轮做残留清理 |
| 071～080 Lease/Fencing | acquire race 1000；takeover/旧 token 边界 E3 5×20 |
| 081～090 分区/迁移 | 冻结 v7 Fixture；中断/损坏窗口×5；TC-090 还需 Artifact/E4 |
| 091～100 Task Contract/Graph | Contract 攻击+Integration；Draft 未 Commit 时 Child/Invocation/副作用均为 0 |
| 101～110 Supervisor/Wait | Reducer replay 1000 Events；Wait/Guidance/Crash 恢复 E3×5 seed |
| 111～120 Evidence/Proof | Schema+伪造/陈旧/矛盾攻击；独立 terminal ground truth；Proof commit E3 |
| 121～130 Capability/Namespace | 交集/路径攻击 E2；Credential/MCP/Process 跨 Job E3×5 seed |
| 131～140 Quota/Fairness | 至少 3 负载台阶×3；Noisy Neighbor 和重启等待进入 Long-run |
| 141～150 性能/复现 | 使用 Long-run/External 合同；GATE-40 绿灯不能替代统计或外部复现 |

### 21.5 W1 与后续 Wave 的预注册 `caseVariant` 清单

以下均是 Draft。只有 `ADR-W1-TURN-001`、分区/Lease ADR 和各自 digest 进入 Accepted 后，依赖满足的条目才可进入实现；在此之前状态为 `BlockedByDecision`。这不改变顶层 150 分母。

#### 21.5.1 W1：同 Thread 顺序与跨 Thread 并行

| Variant | 关联 TC | 场景与 Barrier | 重复 | Oracle / Claim Ceiling |
|---|---|---|---:|---|
| W1-A-001 | 031 | 单 App 两个 `turn/start` 同时到达 Thread compare-and-commit | 5×200 | `maxConcurrentAdvancingTurns=1`；结果符合已冻结 secondTurnPolicy。仅当 ADR=reject 才要求 1 success+1 conflict；只提交一个 Turn/User Item/admission receipt。通知是可重建投影，不作业务 Oracle |
| W1-A-002-E3-SMOKE | 031/061 | 双 App 对同 Thread 同时 start | 5×20 | 持久 owner/generation 唯一；失败请求零业务事实。仅 E3 smoke candidate，不满足 TC-031 的 1000 轮分母 |
| W1-A-003 | 032 | complete 与 cancel 同 Barrier | 5×200 | generation CAS 只允许一个合法终态，重放不倒退；只验证现有终态竞态，不冒充 W2 CancelSpec |
| W1-A-004 | 031/032 | terminal+owner release 与第二 start；在提交前后 kill | 5×200 | terminal 与 release 同一 Thread generation；或间隙只返回 `recovery_required` 并 fact-only reconcile，绝不创建双活 |
| W1-A-007 | 001/002 | 不同 Thread 的 Thread-partition commit 同时进入临界区 | 5×200 | 两者成功；记录 lockPath digest 和 monotonic interval，区间重叠且写集合不含 global Snapshot/Lease/ACTIVE |
| W1-A-008 | 021/022/031 | ack 丢失后重试相同 clientRequestId | 5×20 | scope=`threadId+id`；同 digest 返回原 receipt；不同 digest `idempotency_conflict`；不创建第二 Turn。三个 TC 分别生成 criterion/Proof Fragment |

#### 21.5.2 W1：Job State/Lease 分区与 Fencing

| Variant | 关联 TC | 故障/攻击 | 重复 | Oracle / Claim Ceiling |
|---|---|---|---:|---|
| W1-B-001 | 087 | jobId 含 `..`、分隔符、大小写碰撞、超长 Unicode | 全 Fixture | 安全 jobKey 唯一；路径不能逃出根目录 |
| W1-B-002-E2 | 088 | 单进程/双 writer 对同 Job 使用相同 parent generation | 5×200 | 只有一个 HEAD CAS 成功；另一方冲突。仅 E2 candidate，不单独满足 TC-088 |
| W1-B-002-E3 | 088 | 双 App 竞争 Job A generation，同时第三执行流提交 Job B | 5×20 | Job A 只有一个 HEAD CAS 成功；冲突只影响 A，Job B 正常提交；保存双 PID/lock/HEAD timeline。该条与 E2/Crash variants 汇合后才可能支持顶层 TC-088 |
| W1-B-003 | 062 | Job A/B 同时持有各自 Lease 并提交 | 5×200 | 两者成功；lockPath digest 不同，临界区重叠，写集合不含 global Snapshot/Lease/ACTIVE |
| W1-B-004 | 061/064 | 旧 owner 在 takeover 后提交现有 W1 state/journal/HEAD | 5×20 E3 | HEAD swap 前二次校验 token/expiry，旧 owner 100% fenced；CompletionProof/FinalReceipt 在 W1 必须 absent |
| W1-B-005 | 071/073 | acquire、renew、release、expiry、takeover 竞态 | 5×200 | 唯一 owner；fencing token 单调；过 Lease deadline 的 commit 失败。Job CancelSpec 属 W2 |
| W1-B-006 | 088 | per-Job state 写后、manifest 前 process kill | 5 seed | orphan 不可见；HEAD 指向旧唯一 generation；supporting crash evidence only，不单独满足或升级 TC-088，也不冒充 TC-083 迁移 |
| W1-B-007 | 088 | per-Job manifest 写后、HEAD 前 process kill | 5 seed | payload/manifest 可留 orphan但不生效；supporting crash evidence only，不单独满足或升级 TC-088 |
| W1-B-008 | 088 | per-Job HEAD rename 后 process kill | 5 seed | 新 generation 在进程崩溃模型下唯一生效；supporting crash evidence only，不单独满足或升级 TC-088，不外推断电/fsync durability |
| W1-B-009 | 086 | Job A state/manifest/hash 截断或篡改 | 每类×5 | A 隔离/fail-closed，Job B 仍可读写 |
| W1-B-010 | 081 | v7 pure reader/dry-run 连续执行 | 20 | v7与Lease源字节/hash不变；所有对象有唯一目标域、引用闭包完整、unassigned=0；只证明规划，不迁移 |
| W1-B-012 | 088 | 旧全局 Snapshot/Lease 作为 negative control | 5×20 | 用 lock-wait instrumentation 观察全局串行；不能计为新方案通过证据 |

#### 21.5.3 W1：生产 Authority/Event 垂直切片

| Variant | 关联 TC | 场景 | Oracle |
|---|---|---|---|
| W1-C-001 | 021/029 | 同一生产 mutation 重复 eventId | Authority journal 中 eventId 只出现一次；重复请求写新的 rejection auditRecordId 并引用 rejectedEventId，不 append 第二个同 ID 事件 |
| W1-C-002 | 027/028 | 旧 aggregate generation/迟到 predecessor | 业务状态不变，保留拒绝原因和 correlation |
| W1-C-003 | 038/040 | event correlation 指向错误 Job/Turn | fail-closed；不能投递其他 Job |
| W1-C-004 | 073 | Event/Lease/State generation 混用攻击 | 类型/Runtime 校验拒绝；三者不能互换 |
| W1-C-005 | 062 | 通过 App Server 公共 RPC 提交 Job A/B | 生产入口并行；记录 lock/commit interval、Raw State/Event 和写集合 |
| W1-C-006 | 070 | 每轮正常/强杀后的清理 | 无残留 PID、端口、claim、tmp；清理不误删其他 Job |

W1 当前有 24 个 dependency-eligible Draft 变体；实现中每个 Contract/Codec 还要补字段级正反例。24 不是通过目标；ADR 未 Accepted 时仍为 Blocked，不增加任何分数。

#### 21.5.4 后续 Wave 预注册变体（不计 W1 退出）

| Variant | Primary Wave | 关联 TC | 场景 | 当前状态 |
|---|---|---|---|---|
| W2-A-005 | W2 | 041 | Model 请求中完整 CancelSpec，响应迟到 | BlockedBy W2 CancelSpec/late-result ADR |
| W2-A-006 | W2 | 049 | Job A cancel 与 Job B start/complete 并行 | BlockedBy W2 CancelSpec/namespace |
| W2-B-011 | W2 | 083/089 | ACTIVE temp 前/后 kill；绑定 v7+Lease source digests | BlockedBy migration/cutover ADR |
| W6-A-009 | W6 | 048 | 旧 waitId/planRevision 的人类回复 | BlockedBy WaitSpec/Amendment |
| W9-A-010 | W9 | 050 | Final Receipt 后 cancel | BlockedBy CompletionProof/FinalReceipt |

### 21.6 WP↔TC 覆盖缺口与修复要求

1. D11 附录 D 目前是 35 行范围映射，不是逐 WP→Variant→Evidence 机器追踪；
2. 展开现有映射只覆盖 147/150，漏掉三个 P0：
   - TC-035“两 Child claim 同 Task”建议映射 B18/C08/B27；
   - TC-036“两 Task claim 重叠路径”建议映射 B10/D04/D10；
   - TC-039“两人工 resolve unknown”建议映射 B26/C04/E11；
3. 上述映射是 ADR 候选，写入机器账本前必须由总控审核每个 assertion；
4. 每个 WP 至少映射 1 正向、1 fail-closed/故障、1 回归；P0 的并发/恢复/隔离项再要求 E3；
5. 一条 TC 同时覆盖多个 WP 时，每个 WP 必须有独立 criterion/Proof Fragment，不能“一灯多分”；
6. 未来 Validator 必须检查 Wave 唯一 primary 归属、WP↔TC 双向 orphan、命令、Evidence、Proof 和 Reviewer，不再只检查 ID 数量。

### 21.7 测试失败和状态晋级

- 任一双活、旧 token 提交、重复外部副作用、false completion、跨 Job 错投/Abort、混合权威、Instrumentation 丢事件、残留进程/端口/锁，立即停止该条件并保存全部 Raw；
- 失败后允许定位和修复，但禁止删除失败 Seed、临时缩短矩阵或仅保存最后一次绿灯；
- Flake 必须有身份、Seed、窗口、Raw、排除规则和 Reviewer；无法定位的失败降低 Claim；
- `caseVariant passed` 只能形成候选 Evidence；顶层 TC/WP 只有在生产入口、规定层级、重复、Oracle、Raw、Reviewer 和 Completion Proof 全部满足时才能考虑晋级；
- 当前机器 Schema 禁止顶层 Verified，因此本节新增计划后状态仍是 0/150 Verified。

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-08-20 | RT95 executable-test-contract v1.2 | 补齐各层样本/Seed/Oracle/停止规则、150 TC 最低层级、W1 24 个依赖合格 Draft 与 5 个后续 Wave 预注册变体、TC-035/036/039 覆盖缺口；补 TC-088 双 App E3 路径，修正跨 Wave 混入、TC 映射、可选停止和 Evidence 预填；保持 0/150 Verified |
