# God-Agent 工作交接 Prompt（2026-08-25）

> 这是普通工作交接 Prompt：不启用目标模式、不创建 Goal、不要求 Git/PR/merge。

## 接管与硬边界

接管工作区：`D:\练手\agent-learn-god-latest-audit`。先设置：

```powershell
$env:AGENT_SKILLS_PATH='D:\练手\agent-learn-god-latest-audit\skills'
```

中文沟通，保留现有修改。除非用户另行明确授权，不执行 Git fetch/pull/checkout/reset/rebase/commit/push，不创建或合并 PR，不修改依赖、package-lock.json、覆盖分母或阈值，不读取或输出密钥。

禁止把 Fake/Mock、计划、Draft、local pilot、同作者复跑写成真实 Provider、正式实验或独立复现；禁止删测试、排除源码、降低门槛或改评分文字制造 95+。

## 已完成代码切片

### Runtime 恢复协议

- `scripts/runtime-state-recovery-bundle.ts` 已支持私有备份、Manifest、SHA-256、generation、candidate 绑定；
- 支持 clean restore、N+1→N rollback、prepared/committed 双 receipt；
- 拒绝覆盖、篡改、extra、候选漂移、路径逃逸和陈旧 rollback 前置条件；
- `tests/release-readiness-test.ts` 恢复专项 10/10，`npm run check` 通过。

### 科研 Artifact Release

科研智囊已停止写入。版本化 creator/verifier 已绑定源码、Draft 预注册、数据字典、公开派生 receipt、统计、表格、Artifact Manifest、License、Claim Table；拒绝缺失、extra、摘要漂移、绝对/本机路径、secret、版本回退、非递增版本和 formal/external 冒充。

示例 Release：0.1.0，9 文件，SHA-256：`e9b8cf5a82c4dabca512819dd68368693513cf48df8e71a9c2e0f1ebee88329c`；边界 `local-tooling-only-not-formal-or-external`；状态 `Draft / NotIncluded / NotRun / NotVerified / NotReviewed`。RT95 35/35、Reproducibility 9/9、Evidence consistency 7 documents、Discovery 113/113。

### 工程覆盖补强

工程智囊已停止写入。本轮涉及 `tests/agent-loop-test.ts`、`tests/agent-runtime-store-test.ts`、`tests/electron-app-server-client-test.ts`、`tests/openai-responses-test.ts`、`tests/workspace-sandbox-test.ts`、`tests/multi-agent-scheduler-test.ts`、`tests/coverage-hotpaths-handlers-test.ts`。未修改 src 业务源码、依赖、锁文件、覆盖脚本、覆盖阈值或 Git。

## 最后一轮独占门禁可信数字

这些数字来自停止并发写入后的串行实跑，优先级高于仓库内旧文档和旧 `docs/evidence/current-evidence.json`：

| 门禁 | 结果 | 边界 |
|---|---:|---|
| TypeScript | 通过 | 只证明类型检查通过 |
| Test discovery | 113/113 | 正式测试文件全部被 package scripts 覆盖 |
| `npm test` | 780 total；778 pass；2 conditional skip；0 fail | 不能写成 780/780 全通过，最终报告须说明两条 skip 条件 |
| 源码行覆盖 | 26,604/28,672 = 92.7874% | 距 95% 还差 635 行 |
| Loaded source | 116/122 = 95.082% | loaded 子门已过 95 |
| Statistics | 11/11 | 本地统计原语，不等于正式研究结果 |
| Paper evidence | 6/6 | 证明流水线，不等于论文完成 |
| Reproducibility | 9/9 | 本机 Artifact 可复验，不等于第二机器复现 |
| Process Chaos | 17/17 | 专项门禁通过，仍须区分 local pilot 与 formal |

旧快照可能写着 736、735、91.19%；未重新生成并验证前，不得使用旧数字。

## 当前五项正式评分

| 环节 | 分数 | 95+ | 主要硬缺口 |
|---|---:|---|---|
| 工程研究原型 | 94 | 未达到 | 覆盖 92.7874%，无同 SHA 远端 CI/干净 Windows/第二机器证据 |
| 考研复试项目 | 90 | 未达到 | 真人计时彩排 0/3，非作者试讲 0/1，真窗口人工验收未闭环 |
| 科研证据闭环 | 69 | 未达到 | formal Verified=0、正式 Raw=0、真实 Provider=0、外部基线=0、独立复现=0 |
| 论文就绪度 | 47 | 未达到 | 正式结果仍 NotVerified/TODO，缺引用核验和论文级审阅 |
| 生产发行成熟度 | 68 | 未达到 | 官方审计仍有 3 high；无签名安装包、干净机安装、升级回滚和长稳证据 |

本地研究工具成熟度可记录为 92，但不能替代科研 69 或论文 47。

## 接任后的执行顺序

### 1. 重跑唯一可信基线

```powershell
npm run check
npm run test:discovery
npm test
npm run test:coverage
npm run test:statistics
npm run test:paper-evidence
npm run test:reproducibility
npm run test:process-chaos
```

### 2. 完成本机剩余门禁

```powershell
npm run test:electron
npm run security:scan
npm run sbom:generate
npm run doctor
npm run test:evidence
npm run evidence:verify
npm run test:release
npm run release:check
```

`release:check` 预期因官方依赖审计的 3 个 high 返回 BLOCKED/非零；若意外 READY，检查门禁是否失效，不得把 READY 当生产放行。

### 3. 继续补覆盖，不改口径

现有口径目标是至少 `27,239/28,672`（95.00%），当前差 635 行。优先补 `src/app-server/main.ts`、`src/cli/main.ts`、Runtime Store/lease/lifecycle、MCP/命令工具/Electron IPC 的真实行为路径。每个切片必须是行为测试 → 专项测试 → `npm run check` → 正式 coverage；不得空 import。最低验收是连续两次独占 coverage 不低于 95.00%，且分母仍为 122 文件/28,672 行。

### 4. 最终统一材料

只在最终串行门禁后更新 `docs/evidence/current-evidence.json`、`docs/COVERAGE.md`、总账、最终裁决、三份智囊 MD、复试验收表、问答、Demo 和 PPT。所有材料必须使用同一候选、同一采样时间和同一数字，清理旧数字残留。

### 5. 重启 Electron

```powershell
$env:AGENT_SKILLS_PATH='D:\练手\agent-learn-god-latest-audit\skills'
npm run electron:dev
```

记录 Electron main、App Server、Renderer、Runtime state restored、App Server ready。窗口启动不等于 Provider 已验证。

## 复试表达边界

推荐说法：

> 我实现的是一个研究长程 Agent 可靠性的本地 Runtime 原型，重点处理进程崩溃、租约切换、重复投递和外部副作用确认未知。当前本机测试和离线故障门禁已经比较完整，但覆盖率仍是 92.7874%，正式 GATE-40、真实 Provider、外部基线、独立复现和彩排都没有闭环，所以我不会把它说成论文完成态或生产级产品。

禁止说“所有环节已经 95+”“40/40 证明可靠率 100%”“离线 Mock 是真实 Agent 实验”“已实现端到端 exactly-once”“local-ready 就是 production-ready”或“同一作者同一机器复跑就是独立复现”。

## 接任者最终回报格式

```text
1. 本次实际修改的文件：
2. 未修改的敏感范围：依赖 / lockfile / Git / 密钥：
3. 独占门禁结果：check、discovery、main test、coverage、research、Electron、security、SBOM、doctor、release：
4. 最新统一数字与采样时间：
5. 五项评分：工程 / 复试 / 科研 / 论文 / 生产：
6. 仍低于 95 的项目及差距：
7. 不能由本机自动完成的外部条件：
8. Electron 是否已启动，以及 main / App Server / Renderer 状态：
9. 下一切片（只给一个最小可验证切片）：
```

如果任何一项仍低于 95，继续精进；缺少真实外部证据时必须保留 No-Go，不能用文档或评分替代证据。
