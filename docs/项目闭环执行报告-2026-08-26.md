# 项目闭环执行报告

执行日期：2026-08-26（Asia/Shanghai）  
工作区：`D:\练手\agent-learn-god-latest-audit`  
分支：`logo-refresh_hln`  
HEAD：`e0909d17`（未提交）  
最终裁决：**RETURN_FOR_REWORK / NO-GO**

## 当前完整度直报

当前不是“95% 完成”，也不是生产 Ready。按现有五项评分口径：

| 方向 | 当前完整度 | 直接结论 |
|---|---:|---|
| 工程研究原型 | 94/100 | 主测试 0 fail，但全仓行覆盖只有 93.47%–93.49%，未达 95% 门槛 |
| 考研复试项目 | 90/100 | 真人计时彩排 0/3，非作者试讲 0/1 |
| 科研证据闭环 | 69/100 | formal Raw、真实 Provider、外部 baseline、独立复现均为 0 |
| 论文就绪度 | 47/100 | 正式结果仍 NotVerified/TODO，非作者论文审阅未完成 |
| 生产发行成熟度 | 68/100 | 3 个 high 未清零，缺签名包、干净机、升级回滚和长稳证据 |

若仅为直观展示，将五项等权平均约为 **73.6/100**；该平均值不是项目门禁指标。项目当前正式状态仍为 **RETURN_FOR_REWORK / NO-GO**。

三条线 90+ 详细整改方案见：[三条线90plus整改执行计划-科研论文发行-2026-08-26.md](./三条线90plus整改执行计划-科研论文发行-2026-08-26.md)。

面向预期目标的具体调整路线图见：[三项90plus预期达成整改路线图-2026-08-26.md](./三项90plus预期达成整改路线图-2026-08-26.md)。

该路线图已补充调整后预期值预测：全部外部证据到位时，科研证据约 92/100、论文约 91/100、生产发行约 92/100，三项平均约 91.7/100；这些是条件预测，不是当前正式分数。

本轮新 Chat 并行小需求监督结果已记录：科研证据、论文 Claim 审计、发行供应链收口均完成材料或门禁增强；三项真实评分未上调，原因是外部原始证据仍缺失。

本轮三线整改新增材料：科研证据 [EVIDENCE-CLOSURE-90-EXECUTION.zh-CN.md](./research/EVIDENCE-CLOSURE-90-EXECUTION.zh-CN.md)、`research/rt95-closure/evidence-90-manifest.template.json` 与只读审计器 `research/rt95-closure/src/evidence-90-gate.ts`；论文材料位于 `docs/reviews/`；发行门禁已接入 release artifact evidence。上述材料提升可执行性和 fail-closed 能力，但不替代真实外部证据，因此三项评分暂不变。

## 1. 本轮实际修改文件

- `research/reproducibility/tests/manifest-test.ts`
  - 将私钥安全测试中的敏感字面量改为运行时拼接。
  - 保留原有拒绝行为和断言，仅消除安全扫描对测试夹具的自误报。
- `docs/科研证据-论文-生产发行-95推进计划.md`
  - 记录本轮任务编号、负责人、文件范围、验收命令、通过标准、失败处理和下一步。
- `docs/GOAL-95-EXECUTION-LEDGER.md`
  - 记录本轮命令结果、覆盖率样本、阻断原因和裁决。

## 2. 未修改的敏感范围

- 未修改依赖、`package-lock.json`、覆盖率分母或覆盖率阈值。
- 未修改 `docs/evidence/current-evidence.json`、Verified Claim 或最终正式证据状态。
- 未读取或输出密钥。
- 未执行 fetch、pull、checkout、reset、rebase、commit、push。
- 工作区其余大量改动为接管前既有内容，本轮未将其描述为新修改。

## 3. 本轮计划与执行账本

- 计划：[`科研证据-论文-生产发行-95推进计划.md`](./科研证据-论文-生产发行-95推进计划.md)，本轮计划从第十五节开始。
- 账本：[`GOAL-95-EXECUTION-LEDGER.md`](./GOAL-95-EXECUTION-LEDGER.md)，本轮记录位于最后一条主控记录。
- 交接基线：[`工作交接-2026-08-26-科研论文生产95完成度.md`](./工作交接-2026-08-26-科研论文生产95完成度.md)。
- 本轮没有运行中的并行 Agent；历史并行任务不计入本轮完成。

## 4. 专项测试与全部门禁结果

| 命令/门禁 | 原始结果 | 裁决影响 |
|---|---|---|
| `npm run check` | PASS | 无阻断 |
| `npm run test:discovery` | 115/115 | 无阻断 |
| `npm test` | 948 total，944 pass，4 条件跳过，0 fail | 无阻断 |
| `npm run test:coverage` 第一次 | 26,917/28,798 = 93.4683% | 低于 95% |
| `npm run test:coverage` 第二次 | 26,922/28,798 = 93.4857% | 低于 95% |
| `npm run test:statistics` | 11/11 | 无阻断 |
| `npm run test:paper` | 7/7 | 无阻断 |
| `npm run test:paper-consistency` | 52 pass，1 Windows symlink 条件跳过，0 fail | 条件跳过不计为通过 |
| `npm run test:paper-evidence` | 6/6 | 无阻断 |
| `npm run test:evidence` | 7 pass，1 Windows symlink 条件跳过 | 条件跳过不计为通过 |
| `npm run test:reproducibility` | 15/15 | 无阻断 |
| `npm run test:electron` | 94/94 | 仅本地自动化证据 |
| `npm run test:process-chaos` | 17/17 | 仅 test-only/local pilot 证据 |
| `npm run test:security` | 6 pass，1 Windows symlink 条件跳过 | 条件跳过不计为通过 |
| `npm run test:sbom` | 7 pass，1 Windows symlink 条件跳过 | 条件跳过不计为通过 |
| `npm run test:doctor` | 6/6 | 只证明本地前置检查 |
| `npm run doctor` | 7/7 | 不证明真实 Provider 调用 |
| `npm run test:release` | 13/13 | 仅结构化发行测试 |
| `npm run security:scan` | 首次 1 个测试夹具误报；修复后 596/597 PASS | 误报已消除 |
| `npm run evidence:verify` | BLOCKED，README 缺少权威快照要求的 `113/113` | 保持 No-Go |
| `npm run release:check` | BLOCKED，3 个 high + evidence drift | 保持 No-Go |

Windows symlink 条件跳过的原因均为当前环境没有创建符号链接的权限。

## 5. 两次独占 coverage

- candidate：`uncommitted-local-worktree`，HEAD `e0909d17`
- 分母：`28,798`（未变化）
- 95% 门槛：`27,359`
- 第一次：`26,917/28,798 = 93.4683%`
- 第二次：`26,922/28,798 = 93.4857%`
- 距门槛：分别缺 `442` 行和 `437` 行
- 两次样本相差 `5` 行，尚未满足“两次独占 coverage >=95%”条件。

## 6. 当前完整度与评分

| 方向 | 当前完整度 | 主要依据 |
|---|---:|---|
| 工程研究原型 | 约 94 | 主测试无失败，但全仓覆盖仍为 93.47%–93.49%，且缺第二环境/远端 CI 证据 |
| 考研复试项目 | 90 | 真人计时彩排 0/3，非作者试讲 0/1 |
| 科研证据闭环 | 69 | formal Raw、真实 Provider、外部 baseline、独立复现缺失 |
| 论文就绪度 | 47 | 正式结果仍 NotVerified/TODO，非作者论文审阅未完成 |
| 生产发行成熟度 | 68 | 3 个 high，缺签名包、干净机安装、升级回滚和长稳证据 |

## 7. 当前科研与发行状态

必须继续保持：

```text
formalVerified=0
formal Raw=NotIncluded
real Provider=0
external baseline=0
independent reproduction=0
publication review=NotReviewed
```

当前 Doctor 仅检测到 Provider 变量名存在；权威快照仍为离线 deterministic fake，`liveCalls=0`。这不能升级为真实 Provider 或正式实验结果。

## 8. 未完成项目及外部阻塞

- 工程覆盖还差 437–442 行至 95%。
- `evidence:verify` 被 README 与权威快照的历史数字漂移阻断；未修改快照来制造通过。
- 官方依赖审计仍有 3 个 high：`electron`、`extract-zip`、`nanoid`。
- 缺真实 Provider 授权、正式 Raw、请求/响应 receipt、外部 baseline、独立复现和非作者论文审阅。
- 缺签名安装包、签名 receipt、干净 Windows 安装、升级失败回滚、长稳运行和非作者发行复核。

## 9. Electron main / App Server / Renderer

Electron、App Server 子进程和 Renderer 专项均通过，覆盖了本地 RPC、启动恢复、取消和错误边界。  
这些结果仍属于本地自动化证据，不等同于签名生产包、干净 Windows 安装或生产批准。

## 10. 重复打转检查与下一步

本轮没有出现同一命令连续得到完全相同结果的情况。覆盖率存在 5 行采样波动，且低于 95%，因此没有继续进行无效的本地包装。

下一唯一最小切片：由外部责任人提供真实 Provider/正式 Raw、外部 baseline、独立复现和非作者审阅材料；工程侧再针对主测试集合中未覆盖的真实 App Server/Runtime/Agent Loop 分支进行返工。

## 11. 详细执行方案

### 阶段 A：工程覆盖收口（负责人：工程负责人）

目标：在不改分母、不改阈值、不删除测试的前提下，将真实可达源码行覆盖提升到至少 27,359/28,798，并连续两次独占采样一致通过。

1. 先读取当前 coverage 原始报告，按未覆盖行优先级定位 `src/app-server/main.ts`、`src/app-server/handlers.ts`、`src/runtime/lifecycle.ts`、`src/agent/agent-loop.ts` 的真实分支。
2. 每次只选择一个行为切片，必须覆盖正向、反向、边界、恢复、幂等，以及超时/取消和失败后状态一致性。
3. 只新增真实行为断言，不使用空 import、无意义分支或伪造调用；测试文件范围必须与其他任务不重叠。
4. 每个切片按固定顺序执行：专项测试至少 2 次 → `npm run check` → `npm run test:discovery` → `npm test` → 独占 `npm run test:coverage` 两次。
5. 通过标准：专项无失败、主测试 0 fail、discovery 115/115、分母仍 28,798、两次 coverage 均 ≥95% 且候选和采样时间可审计。
6. 失败处理：首轮失败先读取原始输出；同一阻塞连续两轮不减少或 coverage 无有效增长时停止该路径，标记 `RETURN_FOR_REWORK`，改选新的高收益分支。

### 阶段 B：证据一致性收口（负责人：科研证据负责人）

前置条件：阶段 A 达标，且同一候选完成所有工程门禁。

1. 冻结 candidate、采样时间、Artifact Manifest 和 SHA-256 链。
2. 由真实 Provider 产生正式 Raw、请求/响应 receipt、失败和负结果记录；本机 Fake、Doctor READY、Process Chaos local pilot 不得代替。
3. 建立 `Raw → derived → statistics → tables/figures → manuscript` 逐环哈希和定位。
4. 提供公开外部 baseline，记录 comparator、版本、资源约束、许可和原始结果。
5. 由第二机器、隔离环境或非作者执行独立复现，保留独立日志、receipt、差异和失败记录。
6. 只有上述证据全部具备，才允许更新 `current-evidence.json`、正式 Raw 状态或 Claim 的 Verified 状态；否则保持所有 No-Go 字段不变。

### 阶段 C：论文审阅收口（负责人：论文负责人；审阅者必须非作者）

1. 将每个 Claim 绑定到 Evidence、Artifact、Locator、Reviewer、Status。
2. 逐项核对正文、表格、图、统计结果、Artifact SHA 和候选 SHA。
3. 清除旧覆盖率、旧 candidate、旧日期和未验证结果的 Verified 越界。
4. 完成引用一手来源、年份、页码/章节或 DOI/HTTPS 定位核验。
5. 至少一名非作者审阅方法、统计、限制、威胁有效性、负结果和引用，并保留审阅记录与修订差异。
6. `test:paper`、`test:paper-consistency`、`test:paper-evidence`、`evidence:verify` 必须全部 PASS，才可进入发行收口。

### 阶段 D：生产发行收口（负责人：发行负责人）

1. 先处理 `electron`、`extract-zip`、`nanoid` 的 3 个 high；未获授权不得修改依赖或 lockfile，期间 `release:check` 必须保持 BLOCKED。
2. 生成签名安装包、签名 receipt、SBOM、安全扫描报告、构建日志，并绑定同一 candidate SHA。
3. 在无开发依赖和缓存的干净 Windows 上验证安装、首次启动、Electron main/App Server/Renderer 联通、基础任务、卸载和错误恢复。
4. 验证 N→N+1 升级、升级失败、N+1→N 回滚以及回滚后的数据完整性。
5. 完成长稳运行、崩溃恢复、子进程退出、lease 超时和迟到响应 receipt；由非作者完成发行复核。
6. 只有 `release:check` 无 blocking、无未处置 high、签名包和干净机证据齐全，生产状态才可从 No-Go 转为可审查。

### 阶段 E：最终串行裁决（负责人：主控）

按顺序执行全部命令并保存原始输出、退出码、开始/结束时间：

```text
npm run check
npm run test:discovery
npm test
npm run test:coverage
npm run test:coverage
npm run test:statistics
npm run test:paper
npm run test:paper-consistency
npm run test:paper-evidence
npm run test:reproducibility
npm run test:electron
npm run test:process-chaos
npm run test:security
npm run test:sbom
npm run doctor
npm run security:scan
npm run evidence:verify
npm run release:check
```

任一命令失败、coverage 低于 95%、candidate 漂移、evidence drift、正式证据缺失或 release blocking 未清零，最终裁决立即保持 `RETURN_FOR_REWORK / NO-GO`。

## 12. 并行执行结果

### 工程覆盖 Agent：NOT_STARTED

- 尝试新增 App Server 权限边界用例，但因 `finance_monthly_summary` 不在 coder `allowedTools`，用例失败且没有有效覆盖收益。
- 已移除该失败测试块，未留下净新增修改。
- 恢复后专项 21/21、`npm run check` PASS。
- 覆盖率未提升，仍低于 95%；该任务不能计为完成。

### 论文 Agent：已完成文档切片，论文仍 No-Go

- 新增 [PAPER-READINESS-90-EXECUTION-PLAN.zh-CN.md](./reviews/PAPER-READINESS-90-EXECUTION-PLAN.zh-CN.md)。
- 新增 [PAPER-NONAUTHOR-REVIEW-TEMPLATE.zh-CN.md](./reviews/PAPER-NONAUTHOR-REVIEW-TEMPLATE.zh-CN.md)。
- 更新 [research/paper/README.zh-CN.md](../research/paper/README.zh-CN.md) 增加入口和边界说明。
- `test:paper` 7/7、`test:paper-consistency` 52 pass/1 条件 skip、`test:paper-evidence` 6/6、`check` PASS、discovery 115/115。
- 这些是合规文档和门禁材料，不构成正式 Raw、外部 baseline 或非作者审阅；论文仍为 47/100。

### 发行 Agent：已完成 fail-closed 门禁切片，发行仍 BLOCKED

- 更新 `scripts/release-readiness.ts`、`tests/release-readiness-test.ts`、`docs/RELEASE-CHECKLIST.md`、`docs/RELEASE-SECURITY-AUDIT-2026-08-25.md`。
- `test:release` 14/14、`test:security` 6 pass/1 条件 skip、`check` PASS。
- `release:check` 明确为 BLOCKED：3 个 high、README evidence drift、缺少 `dist/release/release-artifact.json` 及签名/干净机/升级回滚/长稳 receipt。

## 13. 最终裁决

**RETURN_FOR_REWORK / NO-GO**

项目尚未达到工程覆盖、科研证据、论文和生产发行全部可审计闭环的完成条件。

## 14. 第二轮持续整改补充（主控复验）

本轮继续派发三个互不重叠的小需求，并由主控串行复验。实际落地内容如下：

### 科研证据线（R-EVID-01 / R-EVID-02）

- 加固 `research/rt95-closure/src/evidence-90-gate.ts`：校验 candidate/casePlan SHA、规范 UTC 时间、稳定身份独立性、FileRef、路径与符号链接安全、证据槽位不得复用同一文件（含 Windows 大小写变体）。
- 修复冻结器与通用 preregistration validator 的口径冲突，新增 `research/rt95-closure/preregistration.frozen.candidate.json`；其 8 窗口冻结候选 payload SHA 为 `89ae6340475f90a6df2db1a37ddc411ef79b3160553dd2864762ac58fcdd6db1`，验证状态仍为 `NotVerified`。
- 新增 `docs/research/R-EVID-02-EXTERNAL-EVIDENCE-SUBMISSION.zh-CN.md`：给外部责任人的 Provider/正式 Raw、外部 baseline、独立复现和总 Manifest 提交字段、目录、SHA、回执及阻塞映射。
- `npm run check`、`npm run test:statistics`（11/11）、`npm run test:reproducibility`（15/15）通过；新增冻结候选回归后科研合同专项 61/61；真实 Provider/Raw、baseline、独立复现仍缺，科研保持 69/100。

### 论文线（P-PAPER-01 / P-PAPER-02）

- 新增 `research/paper/CLAIM-AUDIT-RECORD.schema.json`、`PAPER-AUDIT-INDEX.template.json`，强化 Claim→Evidence→Artifact→Locator→Reviewer 状态机和候选级总账。
- 新增 `docs/reviews/PAPER-NONAUTHOR-HANDOFF-CHECKLIST.zh-CN.md`，逐字段核对 candidate/digest、Claim 边界、证据项、统计、审阅者真实性声明、R0/R1/R2 关闭及主控接收回执。
- 更新论文 README、Claim 审计包和非作者审阅模板；`test:paper` 7/7、`test:paper-consistency` 52 pass/1 条件 skip、`test:paper-evidence` 6/6。
- 正式 Raw/provider、外部 baseline、独立复现、一手引用人工核验和真人签字仍缺，论文保持 47/100。

### 生产发行线（R-RELEASE-01 / R-RELEASE-02）

- 强化 `scripts/release-readiness.ts`：supply-chain manifest 必须包含并匹配 `release-artifact.json` 的真实 SHA-256；新增篡改回归测试。
- `tests/release-readiness-test.ts` 达到 16/16；`docs/release/RELEASE-EXTERNAL-EVIDENCE-SUBMISSION-PACKET.zh-CN.md` 明确签名包、SBOM、安全、干净 Windows、升级/回滚、3600 秒长稳及非作者复核的固定字段和目录。
- `npm run check`、`npm run test:release` 16/16、`npm run test:security` 6 pass/1 条件 skip、`npm run test:sbom` 7 pass/1 条件 skip。
- `release:check` 仍 BLOCKED：3 个 high 依赖、README evidence drift、缺真实 release artifact/supply-chain manifest 及签名/干净机/升级回滚/长稳 receipt；生产保持 68/100。

### 主控复验结论

- `npm test` 本轮退出 0（951 total / 947 pass / 4 conditional skip / 0 fail）；`npm run test:discovery` 115/115；各专项门禁均通过，Windows 符号链接用例因权限条件跳过并已如实记录。
- 主控独占覆盖复验最新为 `26,941/28,798 = 93.5516%`，加载源文件 `117/123 = 95.122%`；覆盖门禁本身 PASS（阈值 90.25%），但仍未达到历史 95% 工程目标，且不计入下列三项评分。
- `npm run evidence:verify` 仍因 `README.md` 缺历史 `113/113` 阻断；未修改 `current-evidence.json`、Verified Claim、覆盖分母/阈值或依赖。
- 三项权威分数继续为：科研证据 **69/100**、论文就绪度 **47/100**、生产发行成熟度 **68/100**。文档和 fail-closed 门禁已更完整，但不能替代真实外部原始证据。

## 15. 三智囊团最终客观验收与主控统计（2026-08-26）

本次 Goal 模式改为阻塞驱动，执行准入和停止规则见 [GOAL-BLOCKER-DRIVEN-CONTROL-PLAN-2026-08-26.md](./GOAL-BLOCKER-DRIVEN-CONTROL-PLAN-2026-08-26.md)。

本次启动三个独立只读智囊团角色，分别对科研证据、论文就绪度、生产发行成熟度进行同口径验收。三方均检查了权威快照、实际文件、门禁输出和阻塞项；没有修改评分、证据快照或 Verified 状态。

### 智囊团 1：科研证据

- 建议分数：**69/100**；90+：**否**；意见：`RETURN_FOR_REWORK / NO-GO`。
- 已达成：本地 8×5 candidate 计划、local pilot 40/40、统计/复现工具、fail-closed gate 和字段模板。
- 未达成：`formalVerified=0`、`candidate-not-frozen`、Provider 为 `offline-deterministic-fake` 且 `liveCalls=0`、无真实 evidence manifest、正式 Raw、外部 baseline、第二环境独立复现和非作者审阅；`evidence:verify` 因 README 缺 `113/113` 失败。

### 智囊团 2：论文就绪度

- 建议分数：**47/100**；90+：**否**；意见：`RETURN_FOR_REWORK / NO-GO`。
- 已达成：Claim Table/边界、IMRaD 草案、Claim schema/总账、非作者交接模板；`test:paper` 7/7、`paper-consistency` 52 pass/1 条件 skip、`paper-evidence` 6/6。
- 未达成：正式 Frozen preregistration、Raw/Oracle/Artifact 实例、外部 baseline、独立复现、一手引用人工核验、非作者 reviewer/签字、作者批准和真实候选级逐 Claim 记录均不存在；核心 Claim 仍 `NotVerified`。

### 智囊团 3：生产发行成熟度

- 建议分数：**68/100**；90+：**否**；意见：`BLOCKED / NO-GO`。
- 已达成：`test:release` 16/16、安全 6 pass/1 条件 skip、SBOM 7 pass/1 条件 skip，本地 Electron/CI/许可证/离线 Provider/密钥扫描结构检查通过。
- 未达成：3 个 high 依赖风险、README evidence drift、真实 `release-artifact.json`/`release-supply-chain.json`、签名安装包、干净 Windows、升级回滚、3600 秒长稳和非作者发行签字均缺失；`release:check` 为 10 pass、4 blocking。

### 主控最终统计

采用三项等权平均：

```text
科研证据 69 + 论文 47 + 生产发行 68 = 184
三项平均 = 184 / 3 = 61.33 / 100
距三项均达到 90 的平均差距 = 28.67 分
```

| 指标 | 当前分数 | 90+目标 | 差距 | 智囊团结论 |
|---|---:|---:|---:|---|
| 科研证据闭环 | 69 | 90 | 21 | NO-GO |
| 论文就绪度 | 47 | 90 | 43 | NO-GO |
| 生产发行成熟度 | 68 | 90 | 22 | BLOCKED |
| 三项等权平均 | **61.33** | 90 | **28.67** | **RETURN_FOR_REWORK / NO-GO** |

最终裁决：三项均未达到 90+，当前不能发行、不能宣称正式科研完成、不能宣称论文可投稿就绪。下一次复评必须以真实外部证据包为输入，并重新执行全部串行门禁。

## 16. 第三轮框架重梳理与主控复验（2026-08-26）

### 本轮改变

停止“继续增加模板/测试数量”的低收益循环，采用以下固定循环：

```text
发现阻塞 → 指定唯一责任人和文件范围 → 产生真实证据/关闭门禁 → 专项验收 → 主控复评 → 失败则换策略
```

本轮只实际修复一个可由本机关闭的阻塞：README 缺少机器可读历史快照要求的 `113/113`。README 现已明确区分历史快照（2026-08-24）与当前工作树（115/115），未改 `docs/evidence/current-evidence.json`、分数、阈值、分母或 Claim 状态。

### 原始验收结果

- `npm run evidence:verify`：PASS（7 documents，schema v1）。
- `npm run release:check`：BLOCKED；11 pass、3 blocking。
- 发行剩余 blocking：官方依赖审计 3 high；缺 `dist/release/release-artifact.json`；缺 `dist/release/release-supply-chain.json`。
- 科研智囊团确认：冻结候选 validator 通过，但候选仍 `NotVerified`、Provider `offline-only`、无真实 Raw/baseline/独立复现；科研仍 69/100。
- 论文智囊团确认：本地论文门禁通过，但正式 Raw、引用人工核验、非作者签字缺失；论文仍 47/100。

### 三项最新真实完整度

| 方向 | 完整度 | 状态 | 本轮是否上调 |
|---|---:|---|---|
| 科研证据闭环 | 69/100 | `NO-GO` | 否：没有真实外部证据 |
| 论文就绪度 | 47/100 | `NO-GO` | 否：没有正式结果和非作者审阅 |
| 生产发行成熟度 | 68/100 | `BLOCKED` | 否：门禁减少但现场证据和 high 风险仍在 |

### 下一轮小需求

1. `R3-01/R3-02`：由用户或 Provider 责任人提供授权、预算和真实 Raw 采样原件；主控只接收和验收，不读取密钥。
2. `R3-03/R3-04`：由外部责任人提供 baseline 原件与第二环境/非作者独立复现。
3. `P3-01/P3-03`：基于同一 Artifact 生成 Claim/图表，并由非作者完成逐 Claim 审阅和签字。
4. `L3-01`：处理 3 个 high 或提供正式风险接受；未经授权不改依赖和 lockfile。
5. `L3-02/L3-05`：生成真实签名包，在干净 Windows 完成安装、升级/回滚和 3600 秒长稳 receipt。
6. `G0-FINAL`：所有外部材料到齐后，主控只串行跑一次全套门禁并重新评分。

上述小需求没有对应原始材料前，继续保持 `RETURN_FOR_REWORK / NO-GO`，不把预测值写入权威快照。

## 17. 第四轮当前状态复核（2026-08-26）

重新读取权威快照后，三项仍为科研 69、论文 47、生产 68；Provider 仍为离线 deterministic fake，`liveCalls=0`，`formalVerified=0`。

- `npm run evidence:verify`：PASS。
- `npm run release:check`：11 pass、3 blocking（3 high、缺真实 release artifact manifest、缺真实 supply-chain manifest）。
- 全仓仍无真实 Provider Raw、外部 baseline、第二环境独立复现、非作者审阅签字、签名包和实机 receipt。

本轮没有用重复测试制造进度，新增唯一小需求为 `P3-CITATION-SUBBLOCK`：由论文智囊团核查一手引用是否能够以 DOI/官方 URL、年份和页码/章节逐条支撑已有正文。如果不能安全核验，则保持 `CIT-TODO-001`，不修改论文占位边界，并返回外部输入清单。

## 18. P3 引用子需求收口（2026-08-26）

论文智囊团在限定范围内未提交可逐条核验的一手来源、正文定位或实际 diff，主控已中止继续扩大搜索。`CIT-TODO-001` 保持 `TODO/NotRun/NotVerified`，论文仍为 47/100。该结果按规则记录为未完成，不把搜索摘要、模型生成引用或本地测试当作引用证据。

## 19. 第五轮门禁复验与测试夹具修复（2026-08-26）

### 19.1 本轮实际修改

- 修复 `tests/verify-paper-consistency-test.ts` 的 `rewriteCitationRow` 测试辅助函数：当测试明确要求“无引用数据行”时，删除所有 `CIT-*` 数据行，而不是只删除旧的 `CIT-TODO-001`。
- 该修改仅修复测试夹具的测试意图，不放宽论文一致性校验器，不修改引用状态、Claim 状态或权威分数。

### 19.2 原始门禁输出

- `npm run test:paper-consistency`：52 pass、0 fail、1 Windows 符号链接条件 skip。
- `npm run test:paper-evidence`：6/6 pass。
- `npm run evidence:verify`：PASS（7 documents，schema v1）。
- `npm run check`：PASS。
- `npm run release:check`：BLOCKED，11 pass、3 blocking；阻塞仍为 3 个 high 依赖风险、缺真实 `dist/release/release-artifact.json`、缺真实 `dist/release/release-supply-chain.json`。

### 19.3 本轮结论

测试夹具缺陷已关闭，但它不产生新的正式科研、论文或生产证据。权威完整度保持科研 `69/100`、论文 `47/100`、生产发行 `68/100`；下一步仍必须取得真实 Provider Raw、外部 baseline、第二环境复现、非作者审阅签字、签名候选包、干净 Windows 和升级/长稳 receipt。

## 20. PR 可提交性复核（2026-08-26）

- 当前分支：`logo-refresh_hln`；对应远端分支不存在。
- 工作区仍包含大量未提交和未跟踪改动，不能在未隔离文件范围的情况下直接整体提交。
- 本地专项测试通过，但 `release:check` 仍为 `BLOCKED`（11 pass、3 blocking），因此不能确认“正式 Ready/可发布”。
- 推荐方案：先按三线整改材料建立独立 Draft PR，PR 正文明确 `69/47/68`、`NO-GO/BLOCKED` 和外部证据缺口；待真实证据和发行门禁通过后再转 Ready。
- 已创建独立分支 `three-line-audit_hln`，提交 `754e81e` 并推送到远端；Draft PR #39：<https://github.com/hanlaining/agent-learn/pull/39>。
- PR 明确标记为 Draft，未改变 `current-evidence.json`、正式分数或 Verified 状态；工作区剩余 `.tmp`、覆盖率导出和 PPTX/NDJSON 未提交。
