# 工作交接：科研论文生产 95%+ 持续推进

更新时间：2026-08-26（Asia/Shanghai）  
工作区：`D:\练手\agent-learn-god-latest-audit`  
当前分支：`logo-refresh_hln`  
工作区状态：存在大量既有未提交修改；本交接不执行 Git 提交、推送、PR、合并或回滚。

## 一句话结论

当前仍未达到“工程、科研证据、论文、生产发行全部 95%+”。工程主测试覆盖最新已确认口径仍约 93.55%，论文正式证据仍 No-Go，生产发行仍因官方依赖审计 3 个 high 和证据漂移 BLOCKED。不能把本机测试、Mock/Fake、Draft、local pilot 或同作者同机器复跑写成正式 Provider、正式实验、外部基线或独立复现。

## 当前可信基线

### 工程研究原型

- 最近一次已完整确认的主测试：`npm test` 931 total；929 pass；2 conditional skip；0 fail。
- 最近两次独占 coverage：`26,941/28,798 = 93.5516%`、`26,919/28,798 = 93.4752%`。
- 行覆盖 95% 门槛：`27,359/28,798`；低样本缺口约 440 行。
- Loaded source：`117/123 = 95.122%`，只说明 loaded 子门过线，不能替代全仓行覆盖。
- Test discovery：`115/115`。
- 后续工程切片已新增 Runtime lifecycle 守卫 30+ 条断言，专项 `6/6`、`npm run check` 通过；全量测试和双次 coverage 正在/待重新锁定，未形成新的最终可信基线。

### 科研与论文生产

- 论文一致性切片新增 Artifact Release 文件集合、字节数、SHA-256、符号链接/额外文件、`releaseSha256`、Claim 逐字段绑定、引用一手来源/年份/HTTPS-DOI 定位门禁。
- 最新专项记录：`test:paper-consistency` 41 tests；40 pass；1 条 Windows symlink conditional skip；0 fail；`npm run check` 通过。
- 纸面-only fixture 未物化完整发布包时应保持原契约；一旦 Claim/Receipt 物化，缺失、漂移、越界、未声明文件必须 fail-closed。
- 必须保持：`formalVerified=0`、`formal Raw=NotIncluded`、`real Provider=0`、`external baseline=0`、`independent reproduction=0`、`publication review=NotReviewed`。

### 发行与安全

- `test:release`：最新切片 13/13 pass。
- `test:security`：6 pass + 1 Windows symlink conditional skip；命令行 security scan 已恢复为 PASS（589/590 candidate files，无高置信凭据）。
- `sbom:generate`：PASS；SBOM 输出路径已改为异步安全路径校验，拒绝工作区外、目录和符号链接输入。
- `doctor`：7/7 READY；只检查 Provider 变量名是否存在，不读取或输出值；不能证明真实 Provider 调用成功。
- `evidence:verify`：BLOCKED，`README.md` 仍残留旧 `113/113`。
- `release:check`：BLOCKED，原因是官方依赖审计 3 个 high + README evidence drift；不能写成 READY。

## 五项评分（沿用最近正式账本）

| 方向 | 当前分数 | 95+ 状态 | 主要缺口 |
|---|---:|---|---|
| 工程研究原型 | 94 | 未达到 | 全仓覆盖约 93.55%；无同 SHA 远端 CI、干净 Windows、第二机器证据 |
| 考研复试项目 | 90 | 未达到 | 真人计时彩排 0/3；非作者试讲 0/1；真窗口人工验收未闭环 |
| 科研证据闭环 | 69 | 未达到 | formal Verified=0、Raw 未纳入、真实 Provider=0、外部 baseline=0、独立复现=0 |
| 论文就绪度 | 47 | 未达到 | 正式结果 NotVerified/TODO；引用核验和非作者论文级审阅未闭环 |
| 生产发行成熟度 | 68 | 未达到 | 3 high；无签名安装包、干净机安装、升级回滚、长稳证据 |

## 并行任务接管状态

| 任务 ID | 负责范围 | 当前状态 | 接任者动作 |
|---|---|---|---|
| `01a0390b-c4cd-79a0-8d60-027cf305b264` | 工程覆盖、Runtime lifecycle/lease | 已补 30+ lifecycle 守卫断言；专项 6/6、check PASS；全量与双次 coverage 尚未收口 | 等待完整输出；若 coverage 仍低于 95%，RETURN_FOR_REWORK；不把专项通过写成全仓达标 |
| `01a0390b-cd4e-7063-8d1e-01b675fe3cb9` | Claim→Evidence→Artifact、引用核验 | 已扩展到 41 tests；曾出现 3 个 fixture 回归，已要求恢复兼容并重跑 | 必须确认 41/40/1 是否稳定；若 fixture 仍失败，先修实现，不改测试期望掩盖回归 |
| `01a0390b-d4d2-7ef3-ad7c-e7425e6f8a77` | release/security/SBOM/doctor | 安全扫描、SBOM、doctor、release 专项通过；release:check 如实 BLOCKED | 检查 diff 是否只在发行/安全范围；确认没有异步 Promise 回归、没有 secret 扫描自误报 |

## 绝对不能做的事情

1. 不更新 `docs/evidence/current-evidence.json`、`docs/COVERAGE.md`、最终裁决或 Claim 的 Verified 状态，除非同一候选、同一采样时间、连续两次 coverage ≥95% 且所有专项门禁重新通过。
2. 不修改依赖、`package-lock.json`、覆盖分母、覆盖阈值；不删除测试、不排除源码、不降低门槛。
3. 不把 `doctor READY`、Electron ready、Process Chaos local pilot 或离线 Mock 写成 Provider 已验证、正式实验、生产放行。
4. 不执行 `git fetch/pull/checkout/reset/rebase/commit/push`，不创建或提交 PR；当前没有主控批准的 PR。
5. 不读取或输出任何密钥。只允许检查变量名存在性；测试凭据必须运行时拼接或使用明显假值，并确保安全扫描不被测试源码自身触发。

## 下一个 Chat 的唯一最小可验证切片

先不要并行扩张范围，按以下顺序收口：

1. 读取三个并行任务的最终输出，检查 `git diff --stat` 和文件越界。
2. 单独运行：`npm run check`、相关专项、`npm run test:discovery`、`npm test`。
3. 独占运行两次 `npm run test:coverage`，记录分子、分母、百分比、采样时间；低于 95% 就退回工程切片返工。
4. 运行 `npm run test:paper-consistency`、`npm run test:paper-evidence`、`npm run evidence:verify`、`npm run security:scan`、`npm run sbom:generate`、`npm run doctor`、`npm run test:release`、`npm run release:check`。
5. 只有全部结果可复验后，才由主控串行追加总账；当前预期仍是 `evidence:verify BLOCKED`、`release:check BLOCKED`，科研和生产保持 No-Go。

## 外部条件清单

本机无法替代以下原始证据：真实 Provider 授权与调用日志、正式 Raw 数据、公开公平的外部 baseline、第二机器或非作者独立复现、非作者论文/引用审阅、真人彩排、签名安装包、干净 Windows 安装、升级回滚和长稳运行。缺任何关键项，论文和生产最终裁决都必须保持 No-Go。

## 交接完成判定

这份文档只表示“当前状态可被下个 Chat 接管”，不表示项目已 95+，不表示论文完成，不表示生产 Ready，也不表示任何 PR 已提交或合并。
